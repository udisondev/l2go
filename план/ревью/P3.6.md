# Реестр находок P3.6

- Задача: P3.6 «Провода игровой ноги, шлюз, энкод-стадия» (план/p3.md)
- Ревью-профиль: S2 — инвариантщик, конкурентщик, безопасник, прагматик, перформансер;
  S8 — + тестировщик, доменщик
- Бюджеты: план 2/2 · рестарты 0/2 · ревью реализации 1/3

## Находки

Слияние раунда 1: инвариантщик (И-F1..F8), конкурентщик (К-Н1..Н10), безопасник (Б-М1..М5,
Б-m1..m4), прагматик (Пр-F1..F7), перформансер (П-М1..М4, П-м1..м4); дедуп — в столбце
«Свидетельство». Сквозные ID F1–F31.

| ID | Стадия | Роль | Утверждение | Свидетельство | Серьёзность | Статус | Решение |
|---|---|---|---|---|---|---|---|
| F1 | S2 | К-Н1 + И-F1 + Пр-F1 + Б-зам. | Внеполосное включение исходящей крипты «вызовом шлюза после Push(KeyPacket)» — гонка с Take/Encrypt write-горутины; аргумент «гонки нет» защищает только эксклюзивность пушей | Трасса: актор Push(KeyPacket)→EnableCrypto (нс), писатель пробуждается и Take→Encrypt видит enabled=true → KeyPacket уходит шифрованным, клиент ждёт plaintext — хендшейк мёртв; типовой порядок, не редкий. Плюс data race по памяти: crypto/game.go:16 `enabled bool` без синхронизации, Enable/Encrypt — две горутины | блокер | закрыта (правкой S4) | S4: марка plain/crypt — свойство элемента FIFO, ставится Push-ером; криптоблок в Take следует марке очереди; стресс Push+Push(crypt) против цикла Take под -race в приёмке |
| F2 | S2 | К-Н4 + Б-М1 | Очередь внутренних событий актора шлюза (кадры/закрытия/таймауты/результаты) не задекларирована: без капа — OOM от 256 ридеров (8-КиБ кадры, сисколл-темп) против одного актора; блокирующая отправка — ридер блокируется (И6); дроп OnClose — утечка состояния/бинда/MaxConns → удалённый DoS | ADR-0003 §4 (декларация очередей: ограничение/политика/метрики); кэпы заданы всем очередям кроме этой; сценарии (а)(б) в К-Н4 | мажор | закрыта (правкой S4) | S4: bounded-канал (ёмкость — дефолт), non-blocking send; frame-событие сверх ёмкости — метрика + разрыв того коннекта; close-событие дропу не подлежит; тест «шторм при остановленном акторе — коннекты рвутся, память ограничена» |
| F3 | S2 | К-Н2 | Результат асинхронной ValidateSession для уже закрытого коннекта создаёт вечный account→conn бинд — удалённо триггеряемый лок-аут аккаунта | Трасса: OnClose в окне валидации (до 3 с) → бинда ещё нет; valid=true приходит на мёртвый connID → бинд без уборщика; каждый следующий вход — GSReasonAccountAlreadyInUse навсегда | мажор | закрыта (правкой S4) | S4: все асинхронные завершения адресованы (connID, поколение); обработчик сверяет живость — мёртвое drop+метрика, без связок; тест закрытия в окне валидации |
| F4 | S2 | К-Н3 | Таймаут персист-ответа не отменяет ожидание; корреляция не специфицирована — поздний ответ может уехать чужому коннекту (Corr повторного запроса при переподключении) | persist-актор надёжен — на каждый запрос придёт поздний ответ после каждого таймаута; Reply.Corr (actor.go:251) спека шлюза не потребовала; мис-роутинг CharList/CreateChar между коннектами одного аккаунта | мажор | закрыта (правкой S4) | S4: ожидания ключуются (connID, Corr); таймаут удаляет ожидание и завершает коннект по close-after-fail; ответ без живого ожидания — dead-letter drop+метрика; тест таймаут→поздний ответ |
| F5 | S2 | Б-М3 + К-Н10 | Судьба коннекта после GSLoginFail не определена; повторный AuthLogin на том же коннекте при валидации в полёте — шторм unary-вызовов на LS с амплификацией (голод валидаций честных игроков, fail-closed для всех); порядок проверок: ValidateSession до бинд-гейта изымает ключи честного повтора | Преобразование F2 P3.4 (мажор там); idle перезаводится кадрами — цикл AuthLogin не ограничен; ValidateSession — однократное изъятие ключей (P3.4) | мажор | закрыта (правкой S4) | S4: GSLoginFail завершает коннект (close-after-fail, атрибуция канона); «валидация в полёте» — состояние окна, повторный AuthLogin вне окна → разрыв; порядок: нормализация → бинд-гейт → ValidateSession |
| F6 | S2 | Б-М2 | Нормализация аккаунта в бинде «один онлайн» не задана — вход «User»/«user» даёт два бинда и две сущности одного аккаунта | Аккаунт едет с провода нетронутым (F24 P3.4: нормализует только стор LS); loginlink-стор ToLower, persist NormalizeLogin — бинд-карта третья keyed-структура без правила; приёмка тестирует один литерал | мажор | закрыта (правкой S4) | S4: нормализация на входе шлюза (ToLower + домен `^[A-Za-z0-9]{3,14}$` по прецеденту persist) до любого использования; кейс смешанного регистра — в приёмку |
| F7 | S2 | Б-М4 | CharacterSelect: домен слота не валидируется — `0D 00 FF FF FF FF` (slot=−1) даёт индексацию вне списка (паника/чужой слот) на аутентифицированном входе | handshake_view.go:115 CharSlot() int32 без границ; спека «валидация имени/слота» — только про CharacterCreate; снимок для EnterWorld берётся из кэша CharSelectionInfo | мажор | закрыта (правкой S4) | S4: валидация слота на применении (0 ≤ slot < count, слот занят) с детерминированным отказом; строка «числовое поле вне домена» в таблицу злых входов |
| F8 | S2 | Б-М5 | Recover-политика актора шлюза отсутствует — паника одного обработчика валит весь процесс (все коннекты); шлюз — наибольшая поверхность недоверенного входа | Соседи обязаны деградировать: регион recover+заморозка (P3.2), persist recover+серия≥3 наружу (P3.3); Go: непойманная паника горутины = падение процесса | мажор | закрыта (правкой S4) | S4: recover на обработку события с деградацией по прецеденту persist (failed-счётчик + slog-алерт, серия PanicLimit → let-it-crash); в приёмку — шов инъекции паники |
| F9 | S2 | Пр-F2 | Переключение read-дедлайнов между фазами (абсолютный HandshakeTimeout → idle 60 с → стационар без дедлайна) не имеет механизма: стейт-машина фаз — в акторе, швов вниз нет; ридэр-прочтения ломают живые коннекты (экран выбора >15 с; стационарный молчун >60 с) либо тащат знание фаз в провода | login-прецедент нерелевантен: там ридэр и стейт-машина — одна горутина (login/conn.go:137); conn.Handler — только события вверх; сценарии (а)(б) разрыва живых коннектов | мажор | закрыта (правкой S4) | S4: нисходящий шов conn (перевод режима чтения: handshake→presession→stationary), вызывается актором при переходах фаз; приёмка: стационарный молчун > IdleTimeout жив, пресессионный молчун — разрыв |
| F10 | S2 | П-М1 + И-F3 + К-Н9(капы) | Backpressure нефальсифицируем: нет дефолтов капов (encode-FIFO в байтах, per-conn inbox, drain-кап, ёмкость канала событий) и нет теста «медленный читатель → кап → disconnect»; механизм disconnect не определён (путь до сокета) | perf.md §3 (bounded + disconnect; TCP flow control не замена); сценарий ~72 МБ/час на зависший коннект; прецедент P3.1 — числа + тест на границе; encode по матрице не импортирует conn — шов разрыва не описан | мажор | закрыта (правкой S4) | S4: именованные дефолты всех капов + валидация >0; disconnect-вердикт из Take (write-горутина рвёт сокет → штатный OnClose); приёмка: юнит-тест капа с фейковым Outbound |
| F11 | S2 | П-М3 + Пр-F3 | «Слэб на кадр из pkg/bufpool (тюнинг — фаза 6)» — Get-без-Put: нарушение контракта пула (bufpool.go doc: Put обязателен; perf.md §1), пул вырожден, датчик деградации постоянно сработавш. ADR-0004 ось 1 и решение 7 говорят иное: слэб на кадр, переиспользование — передача владения стейджу и возврат его очередью | pkg/bufpool/bufpool.go:1–2; ADR-0004:54–62; решение 7 (p3.md:148–150) — три документа согласованы между собой и расходятся со строкой P3.6 | мажор | закрыта (правкой S4) | S4: по букве ADR-0004 — слэб кадра аллоцируется на кадр; владение передаётся стейджу, возврат — очередью стейджа (переиспользование следующим Take той же write-горутины); bufpool из спеки убрать |
| F12 | S2 | П-М4 | Криптоблок без инварианта «покадрово»: единый Encrypt над склеенным батчем ломает протокол (счётчик key[8:12] += len один раз на суммарную длину вместо раза на пакет; каскад prev тянется через границу кадров); предсессионные батчи длины 1 дефект не поймают — отказ отложен в P3.7/P3.8 | crypto/game.go:43–56 (PutUint32 один на вызов xorPass; prev обнуляется на входе); perf.md §1 «пакетная крипта ~2×» провоцирует склейку | мажор | закрыта (правкой S4) | S4: инвариант в спеку: криптоблок = последовательный Encrypt по кадрам батча, единый проход над склейкой запрещён; приёмка: раундтрип батча ≥2 криптованных кадров через реальный GameCrypt |
| F13 | S2 | П-М2 | Бенч-критерий encode («baseline → benchmarks/») не задаёт сценариев/аллок-бюджетов/методологии — бенч родится нечестным (Push без Take; crypto off; один размер кадра; пер-тиковый путь шлюза без бенча или без аргумента) | ADR-0005 п.6 (бенчмарк с первым кодом пути — P3.6 рождает весь encode); прецеденты P3.1/P3.2/P3.5 задают планку сценариев и машинных аллок-проверок | мажор | закрыта (правкой S4) | S4: бенч-семейство (Push→Take раунд 1 кадр и батч 8/32; crypto on/off; 40/256/1456 Б; serial+parallel), машинные бюджеты (0 аллок на Push вне роста и на Take в стационаре, на коалесинг и дрен тика) с оговоркой «недостижимо — фиксация»; методология perf.md §0 |
| F14 | S2 | И-F2 | «Дубль сущности в окне граца разруливает регион (передача P3.7)» — механизма нет ни в P3.6, ни в P3.7 (там только grace и деспавн): обрыв+мгновенный перезаход = две сущности одного аккаунта + гонка персистов (поздний SaveSnapshot старой сущности перезаписывает свежий файл) | Трасса интерливинга в И-F2; спека P3.7 не содержит обработки повторного EnterWorld при живой сущности — спека не может опираться на несуществующий механизм | мажор | закрыта (правкой S4) | S4: специфицировать в P3.7: повторное EnterWorld аккаунта при живой сущности — немедленный деспавн старой (без персиста старой поверх новой), перезапись бинда; критерий — ровно одна сущность, файл отражает вторую сессию |
| F15 | S2 | И-F4 | Метрика латентности стейджа «грубо тиками» без источника тиков — дверь к настенным часам вне шлюза (реализатор возьмёт time.Now() в encode — нарушение D1) | encode не подписан на метроном, Push тика не несёт; прецеденты фазы каждую точку часов вне шлюза аргументируют | минор | закрыта (правкой S4) | S4: латентность-тиками убрать из скоупа; наблюдаемость — глубина (байты/кадры) + счётчики причин |
| F16 | S2 | И-F5 + Пр-F4 | Критерий приёмки checkdeps не включает заявленную S0 правку (gateway ← encode + тестовые l2client/fixture) — трассировка плана в обе стороны нарушена | S0-буллет против последнего пункта критериев; checkdeps.sh:20 сегодня без encode | минор | закрыта (правкой S4) | S4: привести критерий в соответствие |
| F17 | S2 | И-F6 | Handler из трёх методов против буквы ADR-0005 «интерфейсы 1–2 метода» | ADR-0005 п.1; conn.go — сейчас 2 | минор | закрыта (реестровой записью) | Реестровая оговорка: шов жизненного цикла коннекта — один интерфейс трёх событий, полное и минимальное множество; два интерфейса хуже. Правки ADR не требуется |
| F18 | S2 | И-F7 | Полумёртвый сокет держит бинд аккаунта до keepalive-детекта (~105 с) — краш клиента блокирует перезаход владельцу | Числа из самой спеки: idle 60 с + 3×15 с; канон L2J решает вытеснением Kick (заглушка P3.4) | минор | закрыта (правкой S4) | S4: повторный AuthLogin аккаунта с живым биндом — вытеснение: старый коннект закрывается шлюзом, бинд переходит новому (прецедент LS F5 P3.4 «замена со следующей попытки»; сверка канона на КТ); критерий «один онлайн» переформулировать |
| F19 | S2 | К-Н6 | Владение payload на шве OnFrame не специфицировано — нулевая копия из read-буфера ридэра = порча кадра в ящике игрока (data race) | NextFrame — срез в буфере вызывающего; perf.md §1 (копия обязательна при переиспользовании буфера); прецедент owned-копии — l2client/game.go:295 | минор | закрыта (правкой S4) | S4: контракт в спеку conn: OnFrame передаёт владение байтами (свежая копия), ридэр не переиспользует их после возврата |
| F20 | S2 | К-Н8 + П-м3 | Контракт conn.Outbound{Take} не зафиксирован: окно Take-до-регистрации, паркинг-vs-поллинг, выход при закрытии, атомарная публикация записи; FIFO событий одного коннекта (OnOpen до OnFrame) не заявлен; политика пробуждения write-горутины не выбрана (тик-опрос +100 мс латентности предсессии / notify-на-каждый-push = 10⁴ write/с) | conn.go:17–20 без контракта порядка; perf.md §3 (выбор буферизовать/флашить — по p99, зафиксировать); прецедент notify cap-1 пусто→непусто — P3.1 | минор | закрыта (правкой S4) | S4: события одного коннекта — FIFO из одной горутины ридера через одну очередь; Take: пусто — паркинг до push/закрытия, !ok — выход; notify-токен пусто→непусто + Take дренирует всё доступное одним батчем (один write на пробуждение) |
| F21 | S2 | П-м2 + К-Н9(write) | Write-дедлайн стационарной фазы не указан: заблокированный Write с изъятым батчем живёт до keepalive-окна (~105 с) | perf.md §3 «Read/write deadlines на каждом соединении»; байты изъяты из очереди — байтовый кап их не видит | минор | закрыта (правкой S4) | S4: стадийный write-дедлайн по прецеденту login (writeStageTimeout) на каждую запись; окно блокировки ограничено дедлайном |
| F22 | S2 | К-Н5 | Кап per-conn inbox «дроп нового» ломает last-wins коалесинг под лавиной — побеждает устаревшая цель (дрейф → snap-back) | Трасса: Mlast дропнут как новый, коалесинг выбирает Mk | минор | закрыта (правкой S4) | S4: для коалесцируемых типов при переполнении — замена старого (держать последний); метрика раздельно |
| F23 | S2 | Б-m1 | Ключи сессии AuthLogin (4 int32) могут попасть в логи через конвенцию hex-дампа остатка представления | protocol/doc.go:18 конвенция hex-дампа; AuthLoginView отказывает на хвосте — дамп = 16 Б ключей; прецедент F16 P3.4 | минор | закрыта (правкой S4) | S4: строка по образцу F16: ключи/payload AuthLogin не логируются, hex-дамп AuthLogin запрещён; логин/вердикт/адрес — можно |
| F24 | S2 | Б-m2 | Атрибуция канона для переносов P3.6 (KeyPacket(result=0)+разрыв, close-after-fail, порядок фаз) не заявлена в приёмке | ADR-0001 п.2; прецеденты P3.3/P3.5/P3.4-F54 | минор | закрыта (правкой S4) | S4: пункт приёмки по образцу P3.5; источники — в doc-комментариях шлюза/conn |
| F25 | S2 | Б-m3 | Поведение шлюза на persist ok=false (I/O-ошибка записи) не определено — зависание экрана вместо отказа | P3.3: ошибки записи — ответ ok=false с причиной; P3.6 покрывает только таймаут | минор | закрыта (правкой S4) | S4: ok=false → GSLoginFail(GSReasonSystemErrorLoginLater) / CharCreateFail для create-ветки; строка в приёмку |
| F26 | S2 | Б-m4 | Остаточный риск F14 P3.4 (:2106 пассивный слушатель) не продлён на :7777 — on-path читает 4 ключа AuthLogin (KeyPacket-ключ открыт до включения шифрования) | crypto/game.go:20 статическая половина публична; изъятие сессии защищает от повтора, не от рейса первого валидатора | минор | закрыта (реестровой записью) | Реестровая запись риска: продление F14 на :7777, смягчение — doc деплоя (VPN); правки кода нет |
| F27 | S2 | Пр-F5 | Критерий требует от l2client CharCreate, но стадия в клиенте не заявлена (расширение называет только MoveToLocation) | game.go — стадии Handshake/Auth/SelectChar, команды Logout; CharCreate нет | минор | закрыта (правкой S4) | S4: дополнить буллет l2client — стадия создания персонажа по прецеденту SelectChar |
| F28 | S2 | Пр-F7 | IdleTimeout предсессионной фазы 60 с расходится с заявленным источником «прецедент login» (5 мин по канону LOGIN_TIMEOUT) — неторопливое заполнение анкеты >60 с = разрыв | P3.4: idleTimeout 5 мин (канон); ссылка на прецедент в P3.6 неточна | минор | закрыта (правкой S4) | S4: IdleTimeout предсессионной фазы = 5 мин (канон LOGIN_TIMEOUT, атрибуция); HandshakeTimeout 15 с остаётся |
| F29 | S2 | И-F8 + Пр-F6 | Рассинхрон основного раздела спеки с S0: «KeyPacket (генерация ключа…)» и «криптодвижок — на соединение, владение проводов» против ключа-в-conn и крипто-энкодера в стейдже | Два фрагмента одной спеки дают взаимно исключающие указания реализатору | минор | закрыта (правкой S4) | S4: переформулировать основной раздел: ключ из OnOpen; входящий криптоконтекст — у ридера, исходящий — у стейджа |
| F30 | S2 | П-м4 | Нет метрик причин disconnect (кап/протокол/keepalive) и фаз пресессии — инциденты неразличимы | perf.md §3 «метрики по фазам, логи по аномалиям»; F10-механизм без метрики причины неотличим от протокольных разрывов | минор | закрыта (правкой S4) | S4: атомарные счётчики причин закрытия + фазовые счётчики кадров пресессии (дешёвая версия; полная телеметрия — фаза 4) |
| F31 | S2 | П-м1 | Обоснование решения 7 «~2 КБ/тик ≈ весь бюджет на Blowfish» фактически неверно: game-канал — XOR-каскад 2,15–2,5 ГБ/с (0,8–0,9 мс на 2 МБ), Blowfish на исходящем game-канале нет | benchmarks/P1.1-crypto-baseline.txt; решение устоит (владение/порядок/пайплайнинг), цена ложная | минор | закрыта (реестровой записью) | Реестровая запись: уточнить обоснование при первой правке ADR-0004/решения 7 — крипта вне тика по владению и порядку, не по цене XOR; в P3.6 текст не трогать (решение фазы принято) |
| F32 | S5 | И-Н1 + Б-Н2 + Пр-Н1 (×3 независимо) | Вытеснение на бинд-гейте до ValidateSession — неаутентифицированный удалённый разрыв живой сессии по одному имени аккаунта (~40 Б на итерацию, повторяемо) | Трасса: AuthLogin(victim, мусор) → гейт видит бинд → старый закрыт → валидация fails → аккаунт свободен, жертва мертва; плюс несогласованность текста «бинд после валидации» против close «до перепривязки» | мажор | закрыта (правкой S5) | Вытеснение — только при valid=true (в момент установки бинда); pre-auth гейт убран (порядок: нормализация → домен → ValidateSession → бинд/вытеснение); негативный тест «неверные ключи не рвут первого» в приёмке |
| F33 | S5 | К-Н2 + Б-Н4 + Пр-Н2 | Бездроповость close-канала опиралась на неполную посылку: слот, освобождаемый на стороне сокета, при остановленном акторе даёт чурн close-событий > MaxConns → дроп (регресс F2) или блокировка (И6) — non-blocking + bounded + бездроп при чурне несовместимы | Трасса чурна accept → 15-с таймаут → close при стоящем акторе | мажор | закрыта (правкой S5) | Слот резервирует атомарный счётчик на принимающей стороне conn до старта эмиттера (сверх — reject без событий); освобождает только актор обработкой close-события — кумулятив ≤ MaxConns всегда |
| F34 | S5 | все 5 ролей | Правки F11/F15 применены к S0-блоку, но не к пакетному буллету internal/encode и критерию checkdeps — bufpool Get-без-Put и «латентность тиками» вернулись текстуально | план/p3.md:1050–1055 против S0:930–933; критерий checkdeps легализовал ребро encode←bufpool | мажор | закрыта (правкой S5) | Пакетный буллет переписан по S0-форме; bufpool убран из критерия checkdeps |
| F35 | S5 | Б-Н1 + К-Н4 | Close-on-overflow на общем канале бьёт жертву, а не виновника (заполнивший канал атакующий дропает кадр честного → рвётся честный): адресный cross-conn DoS; судьба OnOpen при полном канале не определена | Трасса t0–t4 (Б-Н1); приёмка «коннекты рвутся» не проверяла «чьи» | мажор | закрыта (правкой S5) | Per-conn под-лимит слотов канала (4 = 1024/MaxConns): один коннект не занимает чужие слоты; своё событие сверх своего под-лимита — дроп + закрытие своего сокета (включая OnOpen); приёмка — «рвёт виновника, сосед не страдает» |
| F36 | S5 | К-Н3 | Формулировка FIFO событий ложна на границе кадр→закрытие (close-канал отдельный: OnClose может опередить стоящие кадры) | Трасса OnFrame → смерть ридера → OnClose → select инвертирует порядок | минор | закрыта (правкой S5) | FIFO — цепочке OnOpen→OnFrame; close может опередить кадры — no-op по неизвестному коннекту |
| F37 | S5 | Б-Н3 | Обещанные реестром критерии не попали в приёмку: шов инъекции паники (F8), ok=false-тест (F25) | Греп §P3.6: приёмка молчит; прецедент P3.3 — шов в приёмке | минор | закрыта (правкой S5) | Оба критерия добавлены |
| F38 | S5 | Пф-Н3/Н4 + заметка | Бенч-раунд без возврата слэбов — 0-аллок Push нечестно измерим; ingest-путь актора без бенча; «методология §0» против -count=10 | Push без имитации завершения записи = каждый Push свежая аллокация | минор | закрыта (правкой S5) | Раунд включает возврат слэбов после фейкового завершения записи; под-кейс ingest; формулировка — benchstat-прецедент P3.2 |
| F39 | S5 | Пр-Н6/Н7 + Пф-заметка(б) | «Возврат — очередью стейджа» двусмысленно (второй пушер в FIFO); «по букве ADR-0004» неточно (рециклинг досрочный — требует бюджет); очередь возврата без декларации | Естественное чтение делает write-горутину вторым пушером | минор | закрыта (правкой S5) | «Обмен при Take»; аргумент — 0-аллок бюджет (осознанное опережение фазы 6); декларация очереди возврата (≤ кап FIFO + 1 батч, никогда не блокировать) |
| F40 | S5 | Пр-Н5 | «Поколение состояния коннекта» — лишняя ось: connID вечен/не переиспользуется, суперсессии нет; сверка живости (connID, Corr/тип) равносильна и проще | Разбор сценариев F3/F4: инкремент поколения нигде не нужен | минор | закрыта (правкой S5) | Ось поколений убрана; сверка живости коннекта и ожидания |
| F41 | S5 | Б (косметика) | Паттерн домена расходится с прецедентом persist (`^[a-z0-9]{3,14}$` после lowering) | internal/persist/model.go:20 | минор | закрыта (правкой S5) | Приведено к persist-форме |
| F42 | S5 | И (наблюдение) | Пушер стационарных кадров после P3.7 не назван; проводка мир→encode-FIFO не описана | FIFO допускает параллельных пушеров; P3.7 должна зафиксировать переход пушера | минор | закрыта (реестровой записью) | Обязательство вниз P3.7-S0: после бинда шлюз кадры в encode-FIFO не пушит; пушер стационарных — регион (переход happens-before по KindConnBind) |
| F43 | S5 | Б (процессное) | Смягчение F26 «doc деплоя» не привязано к задаче — риск нематериализации | Реестровая запись без адресата | минор | закрыта (реестровой записью) | Привязка к P3.13 (инструкция запуска в журнале приёмки) |

## Отклонённые (память анти-петли)

| ID | Утверждение | Причина отклонения | Раунд |
|---|---|---|---|

## Исправления

| ID | Правка | Аргументация (альтернативы и почему хуже) | Чем проверяется закрытие |
|---|---|---|---|
| F1, F12 | Спека: граница plain/crypt — марка элемента FIFO (ставит Push-ер), криптоблок — последовательный Encrypt по кадрам батча, склейка запрещена | Альтернатива «внеполосный Enable» опровергнута трассой (блокер); марка в очереди упорядочивается самим FIFO — нулевая стоимость; Enable-механизм GameCrypt остаётся для входящего направления у ридера | Стресс Push(plain)+Push(crypt) против цикла Take под `-race`; раундтрип батча ≥2 кадров через реальный GameCrypt (в приёмке) |
| F2 | Спека: декларация каналов актора — события bounded 1024 non-blocking (overflow → close-on-overflow ридэром), закрытия — cap MaxConns (доказуемо бездроповый) | Блокирующая отправка — И6-нарушение; дроп close — утечка состояния; cap=MaxConns закрывает дроп конструктивно, без ретраев | Тест «шторм при останованном акторе» (в приёмке) |
| F3, F4 | Спека: асинхронные завершения адресованы (connID, поколение), сверка живости; ожидания персиста ключуются (connID, Corr); таймаут удаляет ожидание + close; поздний ответ — dead-letter | Дисциплина «поколение» вместо точечных флагов — одно правило для валидаций/таймеров/писем; ключование по Op/аккаунту опровергнуто трассой мис-роутинга | Тесты асинхронных окон (в приёмке) |
| F5, F18 | Спека: GSLoginFail завершает коннект (close-after-fail); AuthLogin при валидации в полёте — вне окна → разрыв; порядок нормализация→бинд-гейт→ValidateSession; повторный AuthLogin с живым биндом — вытеснение старого | Close-after-fail — прецедент P3.4 и канон; отказ-без-разрыва плодит шторм; вытеснение устраняет и 105-с зомби-блокировку (F18), и согласовано с LS-вытеснением F5 P3.4; сверка канона — на живой КТ | Тесты окон и вытеснения (в приёмке) |
| F6 | Спека: нормализация аккаунта на входе шлюза (строчными + домен persist) до любого использования | Единый ключ для бинд-карты/валидации/писем; два других стора (LS, persist) уже нормализуют — третий без правила давал обход регистром | Тест смешанного регистра (в приёмке) |
| F7 | Спека: валидация слота CharacterSelect (0 ≤ slot < count, занят) на применении | Домен на применении — правило проекта; ранняя валидация на проводе — знание мира в проводах | Строка «числовое поле вне домена» в таблице злых входов |
| F8 | Спека: recover-политика актора по прецеденту persist (failed + серия PanicLimit → let-it-crash) | Шлюз — максимальная поверхность недоверенного входа; «падает процессом» без recover — недопустимая цена одного бага; полный надзор — фаза 4+ | Шов инъекции паники в тестах актора |
| F9 | Спека: нисходящий шов режимов чтения (handshake→presession→stationary), вызывается актором при переходах фаз | Ридэр-с-знанием-фаз — протокол в проводах; таймеры у актора — размазанное состояние; режим — одно поле коннекта, переводят переходы стейт-машины | Тесты молчунов обеих фаз (в приёмке) |
| F10 | Спека: именованные дефолты капов (encode-FIFO 256 КиБ, inbox 64 кадра, drain-кап 8, канал событий 1024) + disconnect-вердикт из Take | Числа — стартовая калибровка (тюнинг фаза 4); disconnect через Take — write-горутина владеет сокетом, encode conn не импортирует | Юнит-тест капа с фейковым Outbound (в приёмке) |
| F11 | Спека: слэб на кадр — аллокация; владение стейджу, возврат очередью стейджа; bufpool убран | По букве ADR-0004 ось 1 и решения 7; bufpool с Get-без-Put — нарушение контракта пула и вырождение пула; полный раунд Get/Put — рециклинг фазы 6, раньше времени | Машинный 0-аллок бюджет на Take в стационаре (бенчи) |
| F13 | Спека: бенч-семейство (Push→Take 1/батч 8/32 × crypto on/off × 40/256/1456 Б × serial/parallel) + машинные аллок-бюджеты + методология | По прецедентам P3.1/P3.2/P3.5; одиночный бенч без сценариев нечестен (Push-без-Take, crypto-off) | Бенчи + baseline в benchmarks/ |
| F14 | Правка спеки P3.7: повторное EnterWorld при живой сущности — немедленный деспавн старой без её персиста + перезапись бинда; критерий в P3.7 | Спека не может опираться на несуществующий механизм; «персист старой поверх новой» — откат прогресса, запрет дублирования важнее сохранения снимка обрыва | Критерий P3.7 (обрыв+перезаход в grace → одна сущность, файл — вторая сессия) |
| F15 | Спека: латентность-тиками убрана; наблюдаемость — глубина + счётчики причин | Источника тиков у стейджа нет; time.Now() в encode — нарушение D1; глубина достаточна фазе 3 | Отсутствие часов в encode — ревью S8 |
| F16 | Спека: критерий checkdeps дополнен (gateway ← encode + тестовые l2client/fixture, правка ADR-0005/checkdeps в задаче) | Трассировка в обе стороны | Ворота checkdeps после S7 |
| F19, F20, F21 | Спека: контракт шва conn — FIFO событий одного коннекта, владение байтами у получателя, паркинг Take + notify пусто→непусто + один батч на пробуждение, стадийный write-дедлайн | Прецеденты: notify cap-1 (P3.1), owned-копия (l2client), writeStageTimeout (login) | Контрактные тесты conn; `-race` |
| F22 | Спека: переполнение inbox коалесцируемого типа — замена старого (last-wins держится под лавиной) | Дроп нового ломает инвариант «последний побеждает» именно в условиях лавины — теряется смысл коалесинга | Тест коалесинга с переполнением (в приёмке) |
| F23 | Спека: ключи/payload AuthLogin вне логов, hex-дамп AuthLogin запрещён | По прецеденту F16 P3.4; дамп остатка представления = 16 Б ключей | Ревью S8 (греп лог-вызовов) + doc шлюза |
| F24 | Спека: пункт атрибуции в приёмке (KeyPacket(result=0)+разрыв, close-after-fail, LOGIN_TIMEOUT, порядок фаз) | ADR-0001 п.2; прецеденты P3.3/P3.5 | Doc-комментарии в S7 |
| F25 | Спека: persist ok=false → GSLoginFail(SystemErrorLoginLater) / CharCreateFail | Детерминированный отказ вместо зависания экрана; mirror таймаут-политики | Таблица злых входов/актор-тесты |
| F27 | Спека: l2client + стадия создания персонажа (NewChar→CharacterCreate→Ok/Fail) | Критерий приёмки требует CharCreate — стадия по прецеденту SelectChar | Интеграционный тест |
| F28 | Спека: IdleTimeout пресессии = 5 мин (канон LOGIN_TIMEOUT, атрибуция) | 60 с рвёт неторопливую анкету; «прецедент login» = 5 мин, расхождение без аргумента | Тест молчуна пресессии |
| F29 | Спека: основной раздел согласован с S0 (ключ из OnOpen; входящий криптоконтекст у ридера, исходящий — у стейджа) | Взаимно исключающие указания реализатору | Ревью текста S5/S8 |
| F30 | Спека: метрики причин закрытия + фазовые счётчики кадров пресессии | perf.md §3; дешёвая версия — атомарные счётчики | Метрики в Stats + тесты |
| F17, F26, F31 | Без правки спеки — реестровые записи: F17 — оговорка о шве жизненного цикла (один интерфейс трёх событий, полное и минимальное множество); F26 — продление остаточного риска F14 P3.4 на :7777 (on-path читает ключи до включения шифрования; смягчение — doc деплоя); F31 — уточнение обоснования решения 7 (крипта вне тика по владению/порядку, не по цене XOR; текст решения — при первой правке ADR-0004) | — | Настоящий реестр |

## Дофикс-1 (S8-р1, закрывает F47–F63)

| ID | Правка | Проверка |
|---|---|---|
| F47/F51/F52 | conn: дедлайны реструктурированы — перевооружение при SetReadMode (прерывает заблокированный Read), перезавод idle только ПОСЛЕ полного кадра (dribble не продлевает), вершина цикла дедлайны не трогает (Server.Close-дедлайн не стирается) | тесты молчунов: handshake-абсолют переживается экраном выбора, стационарный молчун жив, пресессионный молчун рвётся |
| F48 | tornDown-маркировка teardown'ов актором; teardown из close-события не маркирует (некому гасить) | TestGatewayChurnNoGrowth: оба пути чурна, карты пусты |
| F49 | pending{op, seq, timer}: монотонный seq ожидания, completion несёт seq, timer.Stop() в ответе/teardown; валидационная горутина без дефолт-дропа | TestGatewayPersistTimeout (устаревший таймер → dead-letter) |
| F50 | CharList ok=false → GSLoginFail(SystemError) | TestGatewayPersistRefusal |
| F53 | метрики: conn.Server.Stats (причины закрытия EOF/timeout/protocol/cap/overflow/crypto/keygen + дропы канала), encode client.Depth (байты/кадры), gateway Stats: атомики conns/bound + PhaseFrames | Stats-поля; тест капа с причинами |
| F54 | replyCreate: CharCreateOk + свежий CharSelectionInfo (канон initNewChar); l2client.CreateChar возвращает записи | TestGatewayFullFlow без реконнекта |
| F55 | CHARACTER_DELETE → CharDeleteFail(DeletionFailed), коннект жив | белый список + писатель |
| F56 | раса/класс create-view сверяются с шаблоном → CharCreateFail(CreationFailed) | фазз + злые входы |
| F57 | единый persistRequest(gc, req) | код |
| F58 | persist.Reply.Code (стабильные коды name_invalid/name_taken/char_limit/appearance/login/io); gateway маппит коды | TestCreateFailReasonMapping |
| F59 | Stats: атомики connsN/boundN | код |
| F60 | бенч дренирует faf-ящики (Send — полный путь) | baseline рев. 2 |
| F61 | baseline рев. 2: -benchtime=100ms -count=10 | benchmarks/ |
| F62 | BenchmarkPushTakeParallelCrypt (4 пушера RunParallel-циклом) | baseline |
| F63 | тесты: recover-шов (testPanicOn) + серия PanicLimit; смешанный регистр (case-fold, один бинд); сосед-свидетель шторма; churn; крипто-стресс plain+crypt (марки по длине провода); ok=false list; 8-й персонаж TooMany; createFailReason-юнит; фазз по всем фазам | все зелёные |
| Миноры | rand-отказ эмитит закрытие (слот не течёт); LOGIN_TIMEOUT/KeyPacket.java/close-after-fail атрибуция; l2client+tap: ключи вне логов (golden обновлены), пустые кадры guard; Logout→command; ActClose-doc; фазовые отсылки вычищены; мёртвое closing-поле; GSReasonNoText литерал; SessionID=playOk1; activeSlot=lastSeen; GameTime-комментарий; OpCCharacterDelete в белом списке | греп/ревью |

Остаточные записи: CharID=слот (канон — глобальный id; КТ-риск, P3.7-S0 — рассмотреть стабильный
id персиста/EntityID в слитке); keepalive interval/probes требуют ListenConfig.Control — фаза 4;
CompletionsCap ≥ MaxConns — обязательство wire-up (cmd, P3.7).

## Раунды

- Раунд 1 (S2, план): инвариантщик, конкурентщик, безопасник, прагматик, перформансер —
  слепо, параллельно. Слияние: 1 блокер (F1), 13 мажоров (F2–F14), 17 миноров (F15–F31);
  отклонённых нет. Бюджет ревью плана: 1/2.
- Раунд 2 (S5, замыкание раунда 1): все 5 авторов подтвердили адресацию исходных находок
  по существу (31/31); вскрыты 4 новых мажора (F32 вытеснение до валидации — ×3 независимо;
  F33 бездроповость close-канала; F34 хвосты bufpool/латентности; F35 несправедливый
  close-on-overflow) и 9 миноров (F36–F43 закрыты правкой/реестровыми записями).
  S5-дофикс: вытеснение при valid=true (pre-auth гейт убран — порядок упростился);
  слот MaxConns резерв до эмиттера/освобождение актором; per-conn под-лимит 4 канала
  событий; пакетный буллет encode и критерий checkdeps дочищены; поколение → сверка
  живости. Микрозамыкание дофикса — ниже. Бюджет ревью плана: 2/2 (исчерпан —
  S6 обязан дать «состоятельно», рестарт = владельцу).
- Раунд S8-р1 закрыт дофиксом-1 (см. таблицу выше): 0 блокеров/мажоров после фикса по
  утверждению ведущей сессии; подтверждение авторов — микрозамыкание. Бюджет S8: 1/3.
  Исходно: сведение с дедупом, ID продолжены (F47+; F44–F46 заняты S6).
  ID продолжены (F47+; F44–F46 заняты S6). Блокер F47 (перевод режима чтения
  не перевооружает заблокированный Read — стационарный молчун детерминированно
  разрывается; ×4 роли). Мажоры: F48 tombstone уже-разобранных коннектов —
  вечная утечка; F49 таймеры персиста не отменяются/не ключуются по ожиданию —
  чужой таймаут рвёт живой коннект; F50 CharList ok=false → пустой список;
  F51 Server.Close может висеть (loop-top стирает прошедший дедлайн);
  F52 dribble-байт перезаводит idle (слоты MaxConns удерживаются);
  F53 метрики F30 не реализованы (глубина FIFO/причины закрытия/фазовые
  счётчики); F54 после CharCreateOk канон шлёт свежий CharSelectionInfo
  (доменщик по Mobius CharacterCreate.initNewChar); F55 CHARACTER_DELETE рвёт
  коннект вместо CharDeleteFail; F56 раса/класс создания тихо подменяются
  шаблоном; F57 дублирование persistRequest/onCreate; F58 контракт причин
  создания на подстроках русских текстов; F59 Stats() без синхронизации;
  F60 TickDrain-бенч меряет дроп-ветку Send (faf-ящики полны); F61 baseline
  benchtime=1000x — CV до 55%, абсолюты +43%; F62 нет бенча параллельных
  пушеров; F63 непокрытые критерии приёмки (инъекция паники, смешанный
  регистр, сосед-свидетель шторма, churn, стресс крипто-границы plain+crypt,
  молчуны-негативы, ok=false, 8-й персонаж). Миноры F64+: rand-утечка слота,
  LOGIN_TIMEOUT/источники атрибуции, l2client ключи в трафик-логе + пустые
  кадры reply[0], ActClose-doc-дрейф, фазовые отсылки в доках (7), мёртвые
  поля/литералы, тест-хелперы vs stdlib, keepalive-шов (интервал/probes —
  запись), SessionID=playOk1 (канон), CharID=слот (КТ-риск, обязательство
  P3.7), GameTime-комментарий, фазз в одной фазе, статусы 🔄. Дофикс-1 ниже.
- Микрозамыкание S5-дофикса: безопасник — F32/F33/F35/F37 адресованы (N1-остаток
  «бинд-гейт до валидации» в пакетном буллете + неквалифицированное «вытеснение» —
  вычищены дочисткой вместе с S0-дублем слэбов, найденным прагматиком); перформансер —
  F34/F38 закрыты, новых нет; прагматик — F40 чисто, F32/F39 закрыты после дочистки двух
  дублей. Возражений против замыкания раунда 2 нет. План финален, вход в S6.
- Раунд S6 (свежий взгляд, 3 сабагента без истории): «состоятельно» 3/3 — рестартов нет
  (бюджет рестартов 0/2). Проверены сшивки с фактическим кодом P3.1–P3.5 (ValidateSession
  сигнатура, Subscribe, persist-кодек, Kind-реестр, checkdeps-allowance, l2client-флоу).
  Наблюдения закрыты на месте: F44 (интерливинг close до обработки OnOpen — утечка записи
  коннекта при churn; правка: close-событие несёт признак доставки OnOpen, поздний OnOpen
  закрытого — no-op; приёмка дополнена churn-критерием), F45 (механизм акторного закрытия —
  close-after-flush маркой в очереди, write-горутина дописывает остаток и рвёт сокет;
  правка в контракт шва), F46 (канал завершений ValidateSession/таймеров — cap MaxConns,
  бездроп по построению; правка в select актора; попутно — косметика критерия смешанного
  регистра: в P3.6 проверяется «один бинд», рождение сущностей — фейк/P3.7). План финален,
  вход в S7.
