# Реестр находок P3.10

- Задача: NPC-население из спавнов статики — internal/world (план/p3.md §P3.10)
- Ревью-профиль: S2 — инвариантщик, доменщик, перформансер, прагматик; S8 — + тестировщик
- Бюджеты: план 1/2 · рестарты 0/2 · ревью реализации 0/3

## Финальный план (S4, раунд 1)

Механика: контрольное письмо разворачивания → Births свёртки → Spawn актором (буква
P3.2: «производители EnterWorld/NPC фаз 3.7/3.10»; «контрольные письма P3.7/P3.10
сведутся к их вызову»). Реплей воспроизводит разворот из письма в логе порций (реплейные
логи — с LogPayloads=true: payload письма разворота и EnterWorld обязателен для повтора
свёртки — обязательство вниз P3.12) + та же статика артефакта.

1. **transport: KindDeployNPCs.** Новый Kind после KindConnBind: контрольное письмо
   региона (Regional()=true, Class=Reliable), payload — transport.NPCDeployMsg
   (JSON-кодек, прецедент EnterWorldMsg): `{CenterX, CenterY, Radius int32}` — режим
   среза один: центр+радиус. Тест полноты реестра Kind — обновить.
2. **world: Region.DeployNPCs(static, cfg).** До старта Run (после — ошибка, повторный —
   ошибка): сохраняет статику региона; encode-валидация payload до отправки; отправка —
   только слепым push через транспорт (И12): `reg.Send(Envelope{To: Addr{Entity: ctrlID},
   FromID: ctrlID, Kind: KindDeployNPCs, Payload: NPCDeployMsg})`. Отправитель и адресат —
   ctrl-ящик региона; валидация на применении: FromID == rules.From, однократность
   (st.NPCDeployed — повторное письмо dead-letter), Radius ≤ 0 → dead-letter; dead-letter
   разворота — slog.Error (диагностика «нулевых NPC» живого прогона), не только счётчик.
3. **world: окружение свёртки.** Группировка статических условий шага: тип
   `Env{Rules Rules; GM *geo.Map; Static *data.Static}` (заполняется в Wire + аргумент
   DeployNPCs), Fold-сигнатура: `Fold(tick, delta, rng, st, ents, portions, adv, env)` —
   8 аргументов (было 9, +static сделал бы 10). Static=nil толерантен: KindDeployNPCs →
   dead-letter. Ребро world→data — уже в матрице ADR-0005.
4. **world: разворачивание deploySpawns (чистая, в foldLetter).**
   - RNG разворота — собственный `rand.NewPCG(regionID, tick)` (D2 — буква спеки
     «RNG от (regionID, стартовый tick)»; изоляция от смещения точек потребления
     остальных стримов свёртки, P3.2/F2).
   - Фильтр среза (один режим): точечный спавн — 2D-дистанция точки до центра ≤ Radius
     (датапак-Z не фильтрует); территориальный — bbox территории ∩ квадрат радиуса
     (консервативно шире круга; следствие — часть NPC вне enter-радиуса, безвредно).
   - На запись: Npc-шаблон lookup (нет — skip-счётчик; fake-players без определения
     исключаются этим же путём — канон SpawnData.checkTemplate); статикой неспавнимые
     типы — skip-счётчик (SiegeGuard, RaidBoss — менеджерные спавны канона,
     SpawnTable.checkTemplate).
   - Count × позиция: Territory=="" → точка как есть, кроме Monster-поддерева: Z-коррекция
     гео NearestZ при |Δz| < 300 (канон Spawn.initializeNpc — монстры на устаревшем
     датапак-Z висят/тонут на живом прогоне); Heading −1 → бросок, иначе &0xFFFF;
     территориальный → Territory lookup (нет — skip), бросок x/y равномерно в bbox до
     попадания в полигон (лимит 64 попыток → skip; канон после 1000 возвращает точку
     вне полигона — наше усиление), banned-полигоны — 3D-проверка по вычисленному гео-Z;
     Z = GM.NearestZ({x, y, midZ}) с клампом в [MinZ, MaxZ] (сид midZ — канон
     ZoneNPoly.getRandomPoint; двусторонний кламп — усиление канона, у него только
     верхний); Heading — бросок rng (канон: Rnd.get(61794) при отсутствии heading,
     территория принудительно −1). periodOfDay (ночные/дневные) — спавним всегда:
     день/ночь-цикла нет, отклонение осознанное.
   - Birth{Ent: Entity{Pos, Heading, HP: 1, Npc: &NpcSkin{...}}}. NpcSkin — поля
     потребителя NpcInfo: TemplateID int32, Name, Title string, Attackable bool,
     CollisionRadius/Height float64, RunSpd, WalkSpd, SwimRunSpd, SwimWalkSpd, PAtkSpd,
     MAtkSpd int32, MoveMult, AtkSpdMult float64. Заполнение из data.Npc: скорости —
     raw-bag `stats.speed.run.ground`/`stats.speed.walk.ground` с floor-дефолтами канона
     run 120 / walk 50 (CreatureTemplate; floor вместо канон-санитизации ≤0 → 0.1 → кадр
     с нулём — отклонение, мотивированное нулевыми скоростями в кадре), Swim = Run/Walk
     (fallback канона), PAtkSpd — `stats.attack.attackSpeed` дефолт 300, MAtkSpd 333,
     MoveMult 1.0, AtkSpdMult 1.1 (дефолт простоя канона CreatureStat); Attackable —
     Type ∈ {Monster, RaidBoss, GrandBoss, Chest, FestivalMonster, RiftInvader,
     EventMonster, FeedableBeast, TamedBeast} (порт L2J_Mobius: setAutoAttackable(true)
     Monster-поддерева; Guard/FriendlyMob/Defender без флага — мирному наблюдателю false).
   - State: NPCDeployed, NPCSkipped (без шаблона/территории/лимит попыток) — счётчики в
     Dump; appendEntity — NpcID NPC-сущностей (машина по дампу обязана отличать NPC);
     slog разворота (deployed/point/territorial/skipped) — актор после applyEffects.
5. **replica: NPC-поля Record.** Без префиксов, примитивы без data-типов (матрица
   зависимостей replica←data не открывать): TemplateID int32, Title string, Attackable
   bool, CollisionRadius/Height float64, RunSpd/WalkSpd/SwimRunSpd/SwimWalkSpd/PAtkSpd/
   MAtkSpd int32, MoveMult/AtkSpdMult float64 (нули у игроков — как Race у NPC).
   recordOf: NPC-ветка заполняет из ent.Npc. Dirty-манифест ADR-0004 не меняется
   (гистерезис — фаза 4, см. F2).
6. **world: NpcInfo-ввод.** composeJoin EventIntroduce для RecordKindNPC: кадр NpcInfo из
   Record (ObjID=ObjectIDBase+Entity, DisplayID=TemplateID, Attackable, позиция/heading,
   все скорости/множители, коллизии, Name/Title, Running=false — канон: NPC не бежит при
   спавне) — WriteNpcInfo; ветка npcIntroduceSkipped умирает (метрика — счётчик вводов).
   Счётчик сущностей региона и NpcInfo при входе — в журнал живого прогона (КТ).
7. **cmd/l2go: wire-up.** static из artifact.LoadFile (сейчас отбрасывается); флаги
   -npc-center "x,y" (дефолт — persist.HumanFighter.Start* стартовая точка новичка,
   единый источник) и -npc-radius (дефолт 20000 ≥ enter 3500; КТ-1: владелец утверждает
   географию по гео-покрытию — конфиг без кода); порядок NewRegion → Wire → DeployNPCs →
   Run.
8. **Датчики/бенчи (обязательства вниз из P3.9; датчик, не ворота).**
   - BenchmarkRegionNPCStep 12k NPC + 100 наблюдателей — с под-бенчами фаз прямым вызовом
     харнесса (aoiStep/фаза A отдельно, прецедент прямого r.step() в bench_test.go):
     obs×N-скан join 12k×100 ≈ 1.2M итераций/шаг впервые измеряется (точка P3.9 шла с
     нулём наблюдателей); фазовые доли — из под-бенчей, в baseline + журнал.
   - Бенч «ввод×наблюдатели»: M наблюдателей рождаются в толпе k NPC, лестница плотности,
     кадры NpcInfo в метрике, ReportAllocs (пик входа в толпу — мотивировка радиуса,
     решение 8).
   - Сверка PublishBlob/FullPass/SteadyDirty против baseline P3.9 (рост Record на NPC-поля).
   - Первый шаг разворота (12k Births: Spawn- вставки, лог порций) — разовая цена,
     датируется в харнессе (StopTimer-обходы), в журнал.
9. **e2e.** Синтетика: спавны рядом со стартовой точкой (точечный + территориальный +
   count + banned + fake-id-без-определения), полигоны фикстуры — целиком внутри или
   целиком вне enter-радиуса (сид живого разворота зависит от тика первого шага —
   пересекающие границу полигоны делают точный count-ассерт флакающим); вход клиента →
   свойственный ассерт «каждый пришедший NpcInfo — в радиусе» по NpcInfoView, полнота
   набора — ассертом свёртки (сид фиксирован); логаут/вход — набор идентичен по id;
   l2client — ожидатель NpcInfo (NpcInfoView готов). Критерий детерминизма «два
   прогона → идентичные позиции» исполняется на уровне свёртки бит-в-бит (дампы двух
   вызовов, фикс. tick+static+письмо; прецедент TestFoldDeterministicBitExact); живая
   сверка — реплей P3.12.

Отклонения от спеки/обязательств (аргументация):

- Гистерезис записи Heading/Z (F91-минор «обязательство вниз при ботах») — ВЫЧЕРКНУТ из
  P3.10 в фазу 4: в задаче мёртв (NPC стоят, игроки меняют X/Y — dirty встаёт до всякого
  порога), а наивная реализация несла два дефекта (сравнение с prev вместо last-sent
  якоря; подавление Z-дельт останавливает пересмотр членства — Z входит в 3D-предикат
  enter/exit). Семантика dirty-манифеста ADR-0004 не тронута.
- Режим «имя зоны» (слэш-альтернатива спеки) — не реализуется: зоны датапака ⊆ круг,
  имя-не-ключ (дубликаты), потребитель один (КТ-1) и выражается центром+радиусом;
  гео-фрагмент владельца может не включать стартовую точку — центр обязан быть
  конфигурируем (в спеке центр фиксирован стартовой точкой). Добавление Zone-фильтра —
  фаза 4 по потребности, шов один (предикат среза).

S0-обязательство «точное число точечных спавнов в полном датапаке»: живой артефакт
владельца на этой машине отсутствует (в репо быть не может — NCsoft-данные); оценка
«порядка 12 тыс.» — решение 8 фазы 3 (обход P2.2). Точное число фиксируется живым
прогоном владельца (slog разворота несёт total/point/territorial/deployed/skipped) —
вопрос в выжимку S9.

## Находки

| ID | Стадия | Роль | Утверждение | Свидетельство | Серьёзность | Статус | Решение |
|---|---|---|---|---|---|---|---|
| F1 | S2 | инвариантщик | гистерезис dirty против prev-поколения, а не last-sent: кумулятивный дрейф ниже порога невидим вечно | blob.go:174 prevRec!=rec; adoptStandingZ допуск 500 юн по +30/шаг никогда не dirty | мажор | подтверждена | п.5 вычеркнут (фаза 4, last-sent там же) |
| F2 | S2 | инвариантщик+перформансер+прагматик | гистерезис в P3.10 досрочен («при ботах» — P3.9/F91), мёртв в задаче, подавление Z ломает событийный детект членства (Z ∈ 3D-предикат enter/exit; ввод/вывод только на changed) | P3.9.md F91 «обязательство вниз при ботах»; join.go:221–226 события только при changed; NPC стоят — подавлять нечего | мажор | подтверждена | п.5 вычеркнут; dirty-манифест ADR-0004 не меняется |
| F3 | S2 | прагматик+доменщик | Zone-режим без потребителя: зоны ⊆ круг, имя-не-ключ, 4 ветки фильтра вместо 2; центр среза неконфигурируем при гео-фрагменте владельца без стартовой точки | data.go:52–61 «имя — не ключ»; спека — слэш-альтернатива; КТ-1 — гео-покрытие | мажор | подтверждена | один режим центр+радиус; -npc-center/-npc-radius; отклонение записано |
| F4 | S2 | доменщик | скоростной блок NpcInfo недоопределён (Swim/MAtkSpd/PAtkSpd/множители — молчаливые нули) | NpcInfoData npcinfo.go:26–34; CreatureTemplate.java:112–149 (дефолты 300/333, swim-fallback) | мажор | подтверждена | полный скоростной блок в NpcSkin/Record (п.4/5 плана) |
| F5 | S2 | перформансер | «фазовые доли» из phAoI-счётчиков неработоспособны (счётчики исполнений 1:1); obs×N-скан join 12k×100 никогда не измерялся (точка P3.9 — нулевые наблюдатели) | region.go:130–136,570 атомики +1/шаг; bench_test.go:216 заполнители без Player → aoiObs пуст | мажор | подтверждена | под-бенчи фаз прямым вызовом харнесса (п.8) |
| F6 | S2 | перформансер | путь NpcInfo-ввода (ввод×наблюдатели, пик входа в толпу) без бенча | ADR-0005 §6 «бенч с первым кодом пути»; прецеденты P3.8-F4/P3.9-F12 | мажор | подтверждена | бенч ввод×наблюдатели, лестница плотности (п.8) |
| F7 | S2 | инвариантщик | «кладёт письмо сам себе» — механизм не зафиксирован, чтение как обход транспорта (И12) | план п.2 формулировка; И12 ADR-0003 | минор | подтверждена | reg.Send(Envelope{To: ctrl, FromID: ctrl}) явно в плане |
| F8 | S2 | инвариантщик | реплей разворота опирается на payload письма, дефолт лога payloads=false | main.go:148 false; P3.2/D5 «payload-байты по флагу» | минор | подтверждена | обязательство вниз P3.12: реплейные логи LogPayloads=true (записано в шапке плана) |
| F9 | S2 | инвариантщик | критерий «два прогона → идентичные позиции» на живом прогоне негарантируем (сид от Now() первого шага) | region.go:521,542 | минор | подтверждена | критерий исполняется на уровне свёртки (п.9); живая сверка — реплей P3.12 |
| F10 | S2 | инвариантщик | Dump не несёт NPC-поля — «машина по дампу региона» неисполнима | appendEntity только Player-ветка (fold.go:606–662) | минор | подтверждена | appendEntity: NpcID (п.4 плана) |
| F11 | S2 | доменщик | Zone Contains с Z отсеивал бы валидные точечные спавны (канон Z не фильтрует, терпит промах до 300) | zone.go:93–108; Spawn.java:429–433 | минор | подтверждена | закрыто вычёркиванием Zone-режима; точечный фильтр 2D |
| F12 | S2 | доменщик | heading −1: канон бросает Rnd.get(61794) (и территориальные); план «→0» — толпа смотрит на север | Spawn.java:461–468, :387 | минор | подтверждена | бросок из RNG разворота (п.4) |
| F13 | S2 | доменщик | сид NearestZ и Z banned-проверки не зафиксированы | ZoneNPoly.java:169 (midZ); NpcSpawnTerritory.java:76–101 | минор | подтверждена | сид midZ; banned по вычисленному гео-Z (п.4) |
| F14 | S2 | доменщик | общий rng шага уязвим к смещению точек потребления будущих писем | fold.go:154 единственный потребитель сегодня; FIFO межгорутинный | минор | подтверждена | выделенный PCG(regionID, tick) разворота (п.4) |
| F15 | S2 | доменщик | Running=true против канона false (NPC не бежит при спавне) | Creature.java:209; AbstractNpcInfo.java:187 | минор | подтверждена | Running=false (п.6) |
| F16 | S2 | доменщик | Record.NpcID как data.NpcID — ловушка матрицы (replica←data отсутствует) | checkdeps.sh:21 | минор | подтверждена | примитивы int32/float64 (п.5) |
| F17 | S2 | перформансер | рост Record на NPC-поля без сверки PublishBlob-лестниц | blob.go:140,174 копия/сравнение полных пейлоадов; baseline P3.9 PublishBlob 10k=1.09 мс | минор | подтверждена | сверка в п.8 |
| F18 | S2 | перформансер | первый шаг разворота 12k Births — разовая цена не датирована | Births ~2 МБ + Spawn×12k + лог; watchdog 3 с | минор | подтверждена | датировка в харнессе (п.8) |
| F19 | S2 | прагматик | Fold на 10 аргументов — сгруппировать статическое окружение | fold.go:151 9 параметров; прецедент StepInput | минор | подтверждена | Env{Rules,GM,Static} (п.3) |
| F20 | S2 | прагматик | -npc-cap без потребителя (объём управляется радиусом) | спека cap не упоминает; codestyle §2 | минор | подтверждена | вычеркнут |
| F21 | S2 | прагматик | NPCSliceMsg — имя не по роли (читается как слайс) | прецеденты EnterWorldMsg/ConnBindMsg | минор | подтверждена | NPCDeployMsg |
| F22 | S2 | прагматик | непоследовательные префиксы NPC-полей Record | blob.go:23–43 без префиксов | минор | подтверждена | без префиксов единообразно |
| F23 | S2 | инвариантщик | смена семантики dirty-манифеста без правки ADR-0004 | ADR-0004 ось 3 | минор | закрыта вычёркиванием | п.5 вычеркнут — семантика не меняется |
| F24 | S6b | тест-планировщик | пик ввода новичка в толпе: O(NPC∩радиус) NpcInfo одной пачкой против stageByteCap 256 КиБ — коннект новичка рвётся (closing при bytes+wireLen > byteCap) | encode.go:57,148; main.go stageByteCap 256<<10; ~1048 кадров порог; дефолт радиуса 20000 разворачивает всё, enter 3500 режет | мажор→минор | подтверждена | механизм порезки — анти-скоуп фазы 3 (per-client outbound budget — фаза 6); запись-отклонение с границами: e2e-фикстура < капа, живой прогон наблюдаем (обрыв явный, не молчаливый), радиус — параметр; вопрос владельцу в S9 |
| F25 | S6b | тест-планировщик | расширение Dump/лога порций (NpcID, NpcSkin, NPCDeployed/NPCSkipped) без bump portionVersion — misparse старых цепочек | portionlog.go:23 portionVersion=3, :432 строгая проверка; твин P3.9-F21/KT4-3 | минор | подтверждена | portionVersion=4 + зеркальные parseEntity/parseState (шов 7) |
| F26 | S6b | тест-планировщик | ожидатель NpcInfo требует экспорта опкода (npcInfo 0x16 неотэкспортирован) | protocol/npcinfo.go:10; прецеденты OpCharInfo/OpUserInfo | минор | подтверждена | protocol.OpNpcInfo (шов 8) |
| F27 | S6b | тест-планировщик | семантика письма разворота при панике шага не запиннена: phaseFold+ — recovered дропает ctrlBatch классово → письмо разворота уничтожено навсегда при однократности, регион молча живёт с нулевыми NPC | region.go:461–468 DropBatch; slog dead-letter ветки не срабатывает | минор | подтверждена | пиннинг: phaseDrain — письмо не изъято, разворот следующим шагом; phaseFold+ — письмо потеряно, recovered() несёт slog.Error при потере неразвёрнутого письма; кейс E7 |
| F28 | S8 | перформансер+практик | BenchmarkNpcIntroDensity мёртв: наблюдатель y=5000 против толпы y≤90 — дистанция >Enter, ни одного NpcInfo; liveness на любых кадрах ложен; числа реестра сняты с мёртвого пути | координаты npc_bench_test.go; CanonJoinConfig 3500; инверсия allocs 100↔1000 в baseline | мажор | подтверждена | наблюдатель в толпе; метрика/живость — только опкод 0x16; уборка наблюдателя вне таймера; baseline перегенерирован |
| F29 | S8 | перформансер | BenchmarkFirstDeployStep: `for _, res := range r.residents { r.Remove(...) }` — Remove сплайсит на месте, range снимает заголовок один раз → пропускает ~половину жителей, накопление/шум GC (разброс 8.9–22.2 мс) | range-семантика vs Remove-сплайн | мажор | подтверждена | снимок ID слайсом до Remove; LogStep 12k Births в измеряемой части (F18) |
| F30 | S8 | перформансер+тестировщик | сверка PublishBlob/FullPass/SteadyDirty против P3.9 не зафиксирована в репо; JoinFullPass 385.7→670.2 мкс (+74%) молча превысил прогноз F17 (+25–30%) | план п.8; файл сверки в /tmp | мажор→минор | подтверждена (понижение: плановая граница была на PublishBlob, FullPass — примирительный путь вне приёмки; стационарный SteadyDirty +25% — в прогнозе) | секция «Сверка» в benchmarks/P3.10-world-npc-baseline.txt (три ряда); обязательство вниз: сжатие Record/delta-пейлоады — фаза 6 (анти-скоуп фазы 3) |
| F31 | S8 | тестировщик+практик | кейс C1 TestRecordOfNPCFields (ПБ) пропущен — NPC-ветка recordOf не фальсифицирована по полям | таблица S6b vs дифф | мажор | подтверждена | TestRecordOfNPCFields: все поля + нули у игрока |
| F32 | S8 | тестировщик+практик | E7/phaseFold вакуумный ассерт: Residents==0 сразу после Failed≥1 всегда истинен | порядок ожиданий | мажор | подтверждена | живые шаги после сброса (KindSeed-будильник; PhaseAck-оракул — при тике 0 doneTick слеп), затем Residents==0 |
| F33 | S8 | практик | гонка харнесса: E2 читает entsProj() при живом Run (латентно: записи r.ents только при чурне населения) | комментарий харнесса «прямой обход — гонка» | минор | подтверждена | quiesce (rCancel+regionDone) перед чтением |
| F34 | S8 | доменщик | deployTerritory: return после неудачного броска теряет сиблингов записи (недоспавн тонких территорий) | npc.go vs SpawnData: независимые Spawn на экземпляр | минор | подтверждена | continue: экземпляр пропущен, запись продолжается |
| F35 | S8 | доменщик | точечный стак: один бросок heading на запись — стопка смотрит в одну сторону; канон бросает per-экземпляр | Spawn.initializeNpc per Spawn | минор | подтверждена | бросок внутри Count-цикла |
| F36 | S8 | доменщик | NpcInfo без оружия рук (RHand/LHand нули): гвардейцы с пустыми руками; данные в raw-bag | AbstractNpcInfo:106–107; equipment.* в датапаке | минор | подтверждена | RHand/LHand: raw-bag → NpcSkin → Record → кадр |
| F37 | S8 | доменщик | двусторонний кламп Z закапывает NPC при гео-Z вне полосы; канон перебрасывает | NpcSpawnTerritory:86–89 | минор | подтверждена | гео-Z вне [MinZ,MaxZ] → переброс (кламп удалён) |
| F38 | S8 | инвариантщик | фильтр среза: квадраты разностей на краях int32 переполняют int64; territorial-размах MaxX−MinX в int32 переносится → Int64N паникует | арифметика npc.go; домен загрузчика — весь int32 | минор | подтверждена | отсечение по осям до квадратов; размах в int64 |
| F39 | S8 | инвариантщик | детектор потери письма неполон (окна: drain после изъятия; паника в fold после NPCDeployedOnce; частичная популяция) | условие phase/NPCDeployedOnce | минор | подтверждена | детектор по факту: письмо в дренированной волне && 0 NPC-жителей → алерт (частичная популяция — ограничение, комментарий) |
| F40 | S8 | инвариантщик | slog разворота в свёртке — отклонение от плана («актор после applyEffects») без записи | план п.4 vs deploySpawns | минор | подтверждена | отклонение записано: единственное место со счётчиками point/territorial, прецеденты slog свёртки, в дамп не входит |
| F41 | S8 | инвариантщик | slog без total — сверка с полным датапаком (S0-обязательство) невозможна | шапка S0 | минор | подтверждена | total в slog |
| F42 | S8 | инвариантщик | E7 phaseDrain: Store после startRun гонится с первым шагом | порядок Store/startRun | минор | подтверждена | Store до startRun |
| F43 | S8 | инвариантщик | -npc-radius int→int32 усечение до валидации (4294967496 → 20000 молча) | main.go | минор | подтверждена | диапазон [0, MaxInt32] до усечения |
| F44 | S8 | инвариантщик | DeployNPCs/Wire TOCTOU (started.Load до записи static) | region.go | минор | подтверждена | конвенция «до старта Run, конкурентный вызов вне контракта» в doc (прецедент Wire) |
| F45 | S8 | прагматик | пачка читаемости: -npc-center свёртка веток; NPCDeployOn производное; r2-имя; banned-флаг+break; ручные офсеты вместо View (E4/npcInfoKnown/Running-байт); мёртвый mod-функтор B12; устаревший deadline e2e; дубли mkStatic/benchStaticOf; вариативный bool startE2E | codestyle §1/§3/§4/§11; testing §1/§6 | минор | подтверждена | закрыто пачкой; variadic bool — отклонение (один вызов с true, doc-комментарий; правка 16 вызовов — шум) |
| F47 | нагрузочный датчик (запрос владельца 2026-09-18) | при 100% одновременно движущихся стрим пар терялся: changed-наблюдатель покрывался полным проходом fullPass, который эмитил только членство/вводы, а dirty-цикл его скипал (covered) — апдейты EventUpdate пары не приходили вовсе; каждый бегущий видел застывших соседей до чьей-то остановки | join.go fullPass member-ветка; фальсификатор: оба движутся → 0 кадров, один движется → кадры есть; вскрыт живостью нагрузочного бенча (0 кадров при 100% движущихся) | мажор | подтверждена | fullPass: member-цель с changed-битом → EventUpdate (полный проход — единственная точка пар covered-наблюдателя); регресс TestJoinBothMovingUpdateDelivered + TestRegionBothMovingStreamDelivered (мутационно краснеют) |
| F46 | S8 | тестировщик | пачка покрытия: B6 точная граница d=R/R+1 и полигон-за-кругом; B8 значения маски 70000→4464/65535; B4 banned-узкий-Z (3D); пустой срез; E3 дамп-оракул; E5 кадры-до-ассерта; E7 phaseB-подкейс; E10 персист-пустота; F2 payload=false-свидетель; литерал 4; H1 живость aoiObs | таблица S6b | минор | подтверждена | закрыто пачкой; territory-miss и id-без-определения недостижимы валидным артефактом (link-валидация P2.2) — защитные ветки, тест невозможен |

## Тест-план (S6b)

Уровни по docs/testing.md §3; «e2e» = полный живой контур in-process (cmd/l2go), «компонентный»
= регион-харнесс с реальными метрономом/транспортом/логом/pushCollector, шаги из тест-горутины.
TDD-порядок: transport → юниты Fold (deploySpawns) → recordOf/composeJoin → компонентные → e2e →
бенчи. Статусы кейсов ведёт S7: планировался → реализован/отклонён (с аргументом).

### Кейсы

**A. transport [юнит]**
- TestKindDeployNPCsRegistry (ПБ): KindDeployNPCs == KindConnBind+1; KindCount без дыр;
  Regional()==true; Class()==Reliable.
- TestNPCDeployMsgCodec (В): roundtrip {CenterX,CenterY,Radius}; мусор → ErrBadLetter.

**B. Fold — разворачивание [юнит, гео через билдер P3.9]**
- TestFoldDeployNPCsPointSpawnAndSkin (ПБ): точечный Monster в срезе → 1 Birth; Z ==
  NearestZ при |Δz|<300; Heading датапак &0xFFFF; NpcSkin полон; HP=1.
- TestFoldDeployNPCsMonsterZCorrectionBoundary (В): |Δz| ∈ {299,300} → коррекция строго <300;
  не-Monster — без коррекции.
- TestFoldDeployNPCsTerritorialBoundsAndCount (ПБ): count=3 → 3 Births в полигоне,
  Z ∈ [MinZ,MaxZ], Heading ∈ [0,65536).
- TestFoldDeployNPCsBannedExcluded (ПБ): позиции вне banned 3D; целиком-banned → 0 +
  NPCSkipped.
- TestFoldDeployNPCsPointCountStack (В): count=3 точечный → 3 в одной точке, ID различны.
- TestFoldDeployNPCsSliceFilterBoundaries (ПБ): d=Radius входит, Radius+1 нет; полигон
  bbox∩квадрат за кругом — разворачивается; int32-края — без переполнения (dx² в int64).
- TestFoldDeployNPCsSkipsTable (ПБ): id-без-определения; fake 80000/89999; RaidBoss;
  SiegeGuard; territory-miss; полигон-игла (64 попытки) — по строке 0 Births + счётчик.
- TestFoldDeployNPCsHeadingRollAndMask (В): −1 → бросок RNG (воспроизводим отдельным
  PCG(regionID,tick)); 70000→4464; 65535→65535.
- TestFoldDeployNPCsNpcSkinSpeedBlockTable (ПБ): raw-bag скорости; без статов — дефолты
  120/50/300/333, Swim=Run/Walk, MoveMult 1.0, AtkSpdMult 1.1; Attackable-таблица типов.
- TestFoldDeployNPCsDeterministicBitExact (ПБ): дампы двух прогонов бит-в-бит; tick+1
  отличается.
- TestFoldDeployNPCsIsolatedRNG (В): Noise следующего шага не зависит от разворота.
- TestFoldDeployNPCsLetterValidation (ПБ): чужой FromID; повторное письмо; Radius 0/<0;
  Static=nil; битый payload → DeadLetters+1, 0 Births, не паника.
- TestFoldDeployNPCsSameBatchWithEnterWorld (В): [Deploy, EnterWorld] одной пачкой — оба
  рождения, порядок писем.
- TestFoldNPCStandingSoANoop (ПБ): 10 шагов — NPC: Beat растёт, Pos/Heading/HP неизменны,
  0 эффектов; foldAdvance не разыменовывает Player.
- TestFoldDeployNPCsNullRegionSemantics (В): emptyGeo — Z = кламп midZ, 0 дропов.

**C. recordOf/replica [юнит]**
- TestRecordOfNPCFields (ПБ): Record.NPC-поля == NpcSkin; Player-поля нули.
- TestReplicaExportedAPISurfaceUpdate (В): go/doc-эталон — примитивы, не data-типы.

**D. composeJoin [юнит]**
- TestComposeJoinNpcInfoIntroduce (ПБ): кадр 0x16, Crypt, Client=Obs.ConnID; NpcInfoView:
  ObjID/DisplayID/Attackable/позиция/Name/Title; Running-байт == 0.
- TestComposeJoinEventKinds (правка, ПБ): старый ассерт npcIntroduceSkipped краснеет → кадр
  + счётчик вводов.
- TestComposeJoinNpcInfoHugeNameTitle (В): 4096 rune — не паникует, View навигирует.

**E. регион-харнесс [компонентный]**
- TestRegionDeployNPCsGuards (ПБ): до Run ок; повторно — ошибка; после Run — ошибка; ровно
  1 письмо To==From==ctrlID.
- TestRegionDeployNPCsBirthsApplied (ПБ): Residents == числу; шаги 2/3 — не растёт; Dump
  несёт NpcID.
- TestRegionDeployDeadLetterCounters (В): Radius≤0 → DeadLetters растёт, Residents==0, не
  заморожен.
- TestRegionNPCIntroduceRadiusSet (ПБ): knownOf 0x16 — множество == NPC в enter 3500; вне —
  ни кадра; по одному вводу, без чурна.
- TestRegionNPCNotObserver (ПБ): NPC не наблюдатели — кадры только игрокам.
- TestRegionNPCReenterSameSetById (ПБ): повторный вход — мультимножество objID идентично.
- TestRegionDeployPanicWindows (В): phaseDrain — разворот следующим шагом; phaseFold —
  письмо потеряно, slog.Error, 0 NPC; phaseB — примирение пере-вводит NPC (F27).
- TestRegionNPCStepPhaseCounters (В): phFold/phAoI/phB растут; стационарные шаги — 0 кадров.
- TestRegionNPCLettersUnderLiveRun (ПБ): живой Run 20 шагов — под -race (CI).
- TestRegionShutdownWithNPCPopulation (Ж): TERM при населении — выход чистый, персист
  игнорирует NPC.

**F. лог порций [юнит]**
- TestPortionLogVersion4Strict (ПБ): версия строго 4; v3 — явная ошибка; roundtrip
  Births-NPC/счётчиков бит-в-бит.
- TestPortionLogReplayDeployDigest (ПБ): LogPayloads=true → реплей ×2 бит-в-бит ==
  живому; перестановка писем меняет дамп; payload=false — свидетель-ошибка.

**G. e2e [полный контур, l2client]**
- TestE2ENpcInfoRadiusOnly (ПБ): каждый пришедший NPC_INFO — дистанция ≤ 3500; счётчик >0
  для внутри-радиусной фикстуры; вне — ни кадра.
- TestE2EReenterNpcSetIdenticalById (ПБ): повторный вход — набор objID идентичен.
- TestE2EDeployDefaultsCenterRadius (В): дефолт центра/радиуса ловится близким/далёким
  спавном.
- TestE2ENpcInfoAfterSlivok (В): первый NPC_INFO строго после USER_INFO.

**H. бенчи (журнал, → benchmarks/)**
- BenchmarkRegionNPCStep (ПБ): 12k NPC + 100 наблюдателей; под-бенчи фаз (aoiStep прямо);
  верификация живости (события/кадры ≠ 0; наблюдатели с Player; Wire в харнессе).
- BenchmarkNpcIntroDensity (ПБ): M×k лестница плотности, кадры в метрике, ReportAllocs.
- BenchmarkPublishBlobNPCRecordGrowth (В): лестница 100/1k/10k против baseline P3.9.
- BenchmarkFirstDeployStep (В): разовая цена 12k Births, StopTimer-обходы.

### Трассировка критериев приёмки

1. машина по дампу (границы/вне banned/ровно count) → B1/B3/B4/B5/B6/B7 + E2.
2. детерминизм → B10 + F2 (бит-в-бит свёртки; живая сверка — P3.12).
3. e2e NpcInfo в радиусе/идентичность по id → G1/G2 + E4/E6 + D1.
4. доли join/SoA в журнал → H1/H2 + E8.
5. ворота → E9 + make check.
Непокрываемых критериев нет; «счётчики в журнал живого прогона» — наблюдаемость журналом
(прецедент О-7 P3.9).

### Анти-план

Гео-примитивы (P2.4/P2.5 покрыты); NpcInfo-писатель побайтово (golden P3.5 — D1 проверяет
маппинг через View); интерналы Publisher/Join (P3.8 неизменна, рост Record — числом H3);
моки (новых интерфейсов нет); slog-строки (оракул — счётчики/Dump); негативное пространство
фазы 3 (periodOfDay, бессмертие, без-AI); реплей исходящего потока; «12k в e2e» — до
разрешения F24 неопределённое поведение; тривиальные аксессоры.

### Швы (обязательство S7)

1. transport.KindDeployNPCs + NPCDeployMsg (JSON) + тесты полноты.
2. Region.DeployNPCs(static, cfg) до Run; отправка reg.Send(To=ctrl, FromID=ctrl).
3. Env{Rules, GM, Static} — аргумент Fold; Static=nil толерантен.
4. Выделенный PCG(regionID, tick) разворота внутри свёртки (не параметр).
5. replica.Record NPC-поля примитивами + recordOf NPC-ветка + doc-эталон.
6. composeJoin NpcInfo-ветка; npcIntroduceSkipped → счётчик вводов.
7. portionVersion=4 + зеркальные parseEntity/parseState (F25).
8. protocol.OpNpcInfo экспорт + l2client ожидатель NpcInfo (F26).
9. Фикстура testdata/synth: спавны у стартовой точки (точечный + территориальный + count +
   banned + fake-id + SiegeGuard/RaidBoss + id-без-определения); полигоны целиком
   внутри/вне enter; NPC-шаблоны со статами и без.
10. Бенч-харнессы: NPC-масса + Observer-игроки с Player; прямые вызовы фаз; StopTimer.
11. knownOf/objIDs-харнесс region_aoi_test.go — расширение на 0x16.

### Исполнение (S7, 2026-09-18)

Все группы реализованы: A (transport), B (deploySpawns; id-без-определения в
мини-датапак невозможен — link-валидация P2.2 отвергает битые ссылки, покрывается
fake-id), C (recordOf + doc-эталон), D (composeJoin NpcInfo + гигант-имя), E
(регион-харнесс: guards/применение/dead-letter/радиус/не-наблюдатели/reenter/
паник-окна/фазовые счётчики/TERM), F (roundtrip v4 + реплей разворота бит-в-бит),
G (e2e: радиус+порядок слиток→NpcInfo; reenter по id), H (бенчи + baseline).
Отклонения от плана кейсов: G3 (дефолт центра/радиуса) не отдельным тестом —
фальсифицирован счётчиком G1 (ровно 6 стартовой окрестности; при чужом дефолте
пришли бы дальние); E9 (-race живой Run) — общим `go test -race ./...`;
E10-TERM — TestRegionShutdownWithNPCPopulation. Юнит-статика — собственный
MapFS-мини-датапак (mkStatic), synth-фикстура расширена спавнами стартовой
окрестности (StartNear/start_terr/StartFar; golden-эталоны data/artifact
обновлены осознанно). Швы 1–11 исполнены; Env сгруппировал и RegionID (сид
разворота). Бенч-числа: benchmarks/P3.10-world-npc-baseline.txt (×10 ряды; числа
раунда S8 после фиксов F28/F29 — см. «Раунды»).

## Отклонённые (память анти-петли)

| ID | Утверждение | Причина отклонения | Раунд |
|---|---|---|---|

## Исправления

| ID | Правка | Аргументация (альтернативы и почему хуже) | Чем проверяется закрытие |
|---|---|---|---|
| F1–F2 | п.5 (гистерезис) вычеркнут из плана | альтернативы: (а) реализовать с last-sent якорем — оптимизация горячего пути без мотивирующей нагрузки (NPC стоят), perf.md §0 нарушен; (б) подавить только Heading — минимальный прок от обязательства «при ботах». Фаза 4 получает формулировку с last-sent | отсутствие пункта в финальном плане; S5-замыкание авторов |
| F3 | Zone-режим вычеркнут, центр конфигурируем | альтернатива — оба режима: +ветки фильтра, +lookup по не-ключу, +OQ — без потребителя; радиус с настраиваемым центром покрывает КТ-1 | финальный план п.1/п.7; S5 |
| F4 | скоростной блок полный (Swim/PAtkSpd/MAtkSpd/множители) | нули в кадре — риск класса KT4-1 на будущее; канон-дефолты бесплатны | финальный план п.4; golden-ассерты полей |
| F5–F6 | под-бенчи фаз + ввод×наблюдатели | сводный шаг не атрибутирует фазы; прямые вызовы харнесса — прецедент | финальный план п.8; benchmarks/ |
| F7–F22 | формулировки плана (reg.Send, payloads-обязательство P3.12, критерий детерминизма, Dump NpcID, 2D-фильтр, heading-бросок, midZ-сид, выделенный RNG, Running=false, int32-поля, сверка PublishBlob, датировка первого шага, Env-группировка, минус -npc-cap, NPCDeployMsg, префиксы) | уточнения без альтернатив | финальный план; S5-замыкание |
| F24 | запись-отклонение с границами (механизм порезки — анти-скоуп фазы 3, фаза 6 per-client outbound budget) | альтернатива — механизм порезки первичного потока в P3.10: против анти-скоупа фазы (r2/p3.md «AoI-производительность — фаза 6») и без мотивирующей нагрузки; сузить дефолт радиуса — прячет риск, не решает | план п.9 границы; живой прогон владельца (обрыв наблюдаем явно); вопрос в S9 |
| F25–F27 | portionVersion=4+парсеры; OpNpcInfo экспорт; пиннинг паник-семантики письма (phaseDrain — следующим шагом; phaseFold+ — потеряно, slog.Error в recovered) | уточнения-швы из тест-плана | швы 7/8 + кейс E7 |

## Раунды

- Раунд 1 (S2→S4, 2026-09-18): панели — инвариантщик (7: 2 мажор), доменщик (7 минор +
  OQ-вердикты 1–7), перформансер (6: 3 мажор), прагматик (6: 2 мажор). Слияние: 23
  находки (мажоры F1–F6 после дедупликации кластеров гистерезиса и Zone). Все
  подтверждены свидетельствами по коду/канону; план переработан (гистерезис и Zone
  вычеркнуты, скорости/датчики/формулировки — в план). OQ-1..OQ-7 закрыты вердиктами
  доменщика (скорости: raw-bag stats.speed.* + дефолты 120/50/300/333, swim-fallback;
  Attackable: Monster-поддерево канона; пороги 256/32 — сняты вычёркиванием гистерезиса;
  Z: сид midZ + кламп; bbox-фильтр 2D; Zone — вычеркнут; центр persist.HumanFighter.Start*,
  радиус ≥ enter 3500). Бюджет ревью плана: 1/2.
- S5 (2026-09-18): все 4 роли подтвердили адресацию своих находок финальным планом;
  новых находок ноль. Примечания: доменщик — лимит 64 попыток полигона против канонных
  1000 для тонких территорий (p≈0.05 → ~3,7% skip) наблюдаем через NPCSkipped на живом
  прогоне, переоткрытие — новым свидетельством; перформансер — last-sent-формулировка
  фазы 4 живёт в реестре P3.10 (носитель обязательства), F91-источник её не содержит.
  Выход из ревью плана.
- S8 раунд 1 (2026-09-18): панель — инвариантщик (7), доменщик (5), перформансер
  (6), прагматик (11), тестировщик (13+таблица трассировки), тест-практик (18).
  Слияние: 5 мажорных кластеров — F28 (мёртвый IntroDensity), F29 (range-splice
  в FirstDeployStep), F30 (сверка не зафиксирована, FullPass +41% молча), F31
  (пропущен C1), F32 (вакуумный E7/phaseFold); миноры F33–F46 (пачками).
  Все верифицированы по коду; F30 понижен до минора аргументом (плановая граница
  была на PublishBlob; примирительный FullPass вне приёмки; зафиксирован в
  baseline-сверке + обязательство вниз фаза 6). Закрытия: прод — F34 continue
  сиблингов, F35 per-экземпляр heading, F36 RHand/LHand (raw-bag→кадр), F37
  кламп→переброс вне полосы, F38 края int32 (осевое отсечение, размах int64),
  F39 детектор по факту, F41 total, F43/44; тесты — C1, точные границы B6/B8,
  banned-Z, пустой срез, E-серия (quiesce/дамп/View/phaseB/персист), F2-свидетель
  payloads, литерал 4, View.Running, объединение staticOfXML; бенчи — наблюдатель
  в толпе + метрика npcInfo-frames/op + живость, снимок ID до Remove, LogStep в
  измераемой части, живость aoiObs; baseline перегенерирован со сводкой и секцией
  сверки (медианы: шаг 12k/100 ≈ 3.4 мс, obs×N ≈ +1.2 мс/100 набл., ввод
  1000-NPC ≈ 482 мкс, разворот 12k ≈ 11.0 мс, PublishBlob +22%/+14%,
  SteadyDirty +21%, FullPass +41% — вне приёмки). Отклонения записаны: slog
  разворота в свёртке (F40), variadic bool startE2E (F45), territory-miss/
  id-без-определения недостижимы link-валидацией (F46). Флак
  TestRegionStressSenders ×2 — совпал с фоновыми бенчами на всех ядрах
  (внешняя нагрузка); 8 прогонов подряд чисто. Бюджет ревью реализации: 1/3. 
- Нагрузочный датчик перемещений (запрос владельца 2026-09-18, лестница
  100/1000/10000/20000): BenchmarkRegionPlayerLoad (crowd/clusters50, step/aoi)
  + benchmarks/P3.10-player-load-baseline.txt. Побочно вскрыт и закрыт мажор F47
  (стрим пар при одновременном движении). Границы: ~5000 движущихся рассредоточенных
  — порог бюджета тика (квадрат join-скана слотов); толпа в одном enter-круге —
  предел ~1300 (кадры O(N²)); решения — сетка ячеек (фаза 4, ADR-0004 ось 6) и
  слэб-пул кадров (фаза 6).
- S8 раунд 2 (2026-09-18): контрольное замыкание по диффу cba6e13..fe2b8f5 —
  панели инвариантщик+доменщик (12/12 закрытий подтверждены, регрессий нет),
  перформансер (F28–F30 подтверждены числами; живость/метрики честные),
  тестировщик+практик (F31–F33/F45 подтверждены мутационными проверками;
  F46 — кроме banned-Z). Новые миноры: Н-а (числа сверки/экстраполяции
  шапки baseline не выводились из рядов), Н-б (битая ссылка F36→F5), NF1
  (banned-Z-оракул не фальсифицировал 2D-деградацию — мутация проходила),
  NF2 (phaseB-подкейс: окно вырождения Store против шага разворота). Все
  четыре закрыты: числа выровнены с рядами (PublishBlob N1000 +23%, N10000
  +16%, SteadyDirty +19%, FullPass +41% вне приёмки), экстраполяции убраны,
  ссылка исправлена, banned-Z усилен рождениями внутри 2D-площади
  (мутационная проверка краснеет), phaseB — барьер PhaseAck≥1. Ворота
  зелёные (build/vet/gofmt/staticcheck/checkdeps/test; -race локально на
  затронутых пакетах + полный прогон реализации). Цикл закрыт: ноль
  подтверждённых блокеров/мажоров (2/3 раунда). P3.10 → 👀 владельцу.
- S6 (2026-09-18): свежий взгляд, 3 независимых сабагента (архитектор Go, доменщик L2,
  тестировщик) — единогласно «состоятельно, лучше не найдено»; рестарт не потребовался
  (0/2). Довешивания приняты в план без рестарта: гео-коррекция Z точечных монстров
  (|Δz|<300 — канон Spawn.initializeNpc), фильтр статикой неспавнимых типов
  (SiegeGuard/RaidBoss — SpawnTable.checkTemplate), AtkSpdMult 1.1 (CreatureStat),
  guard Radius≤0, slog.Error dead-letter разворота + encode-валидация payload в
  DeployNPCs, анти-флаки e2e-фикстуры (полигоны целиком внутри/вне радиуса; полнота —
  свёрткой), periodOfDay — осознанное отклонение. Обязательство вниз P3.12: отпечаток
  статики (hash артефакта) в заголовке лога порций — детектор рассинхрона реплея.
