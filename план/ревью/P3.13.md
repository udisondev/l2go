# Реестр находок P3.13

- Задача: p3.md §P3.13 «Приёмка живым клиентом» (КТ-5)
- Ревью-профиль: облегчённый режим — самопроверка S1 + ворота + один ревьюер-роль
  (**безопасник**: доминирующий риск задачи — гигиена данных живого прогона: секреты
  тап-журнала, производные фикстуры, лицензионная граница NCsoft-данных) + владелец.
  Спец-агенты конвейера (тест-планировщик S6b, тест-практик S8) остаются обязательными
  по фьюзам ADR-0006/skill review; толкование: «один ревьюер» ограничивает ролевую панель,
  но не отменяет спец-агентов.
- Бюджеты: план 0/2 · рестарты 0/2 · ревью реализации 0/3

## Находки

| ID | Стадия | Роль | Утверждение | Свидетельство | Серьёзность | Статус | Решение |
|---|---|---|---|---|---|---|---|
| F1 | S6b | тест-планировщик | Объём S7 не включает правку .github/workflows/ci.yml, но embedded-ветка диспатча тестируема только прогоном `-tags embedded` там, где лежит артефакт (embed-шаг), а Makefile-guard — только при наличии make (CI); без этого два критерия фазы A без автоматического оракула | ci.yml:40–53 — embed-шаг гоняет `go test -tags embedded` только для ./internal/artifact/, cmd/l2go под тегом лишь компилируется; локально make отсутствует | мажор | закрыта (правкой) | embed-шаг расширен tag-группой `go test -tags embedded ./cmd/l2go -run TestLoadStatic|TestReplayRequires|TestBootstrapEmbeddedEmptyArtifact`; новый CI-шаг «Run-цели: guard без bin/» |
| F2 | S6b | тест-планировщик | Happy-path run-целей (реальный запуск LS+GS через make) автоматизировать в CI хрупко (топология, останов процессов) | класс «нагрузочные пики/чурн» чеклиста эксплуатации; прецедент P3.4 CI-смоука | минор | закрыта (правкой) | Носитель — живой прогон владельца (фаза B); зафиксировано в журнале-инструкции |
| F3 | S6b | тест-планировщик | Guard «dev: пустой -artifact → ошибка» текущим тестом не покрыт (только косвенно через bootstrap-валидации) | cmd/l2go: нет прямого кейса на пустой путь статики | минор | закрыта (правкой) | TestLoadStaticDevRequiresPath |
| F4 | S6b | тест-планировщик | Фальсификатор подмены источника (S5) требует второй артефакт synth-без-гео | сверка только с LoadFile не фальсифицирует мутацию «embedded всегда вшитые» | минор | закрыта (правкой) | TestLoadStaticEmbeddedNonEmptyPrefersFile строит артефакт-без-гео сам (artifact.Build, nil-гео); доп. зависимости CI нет |

## Тест-план (S6b)

Объём нового кода фазы A — стартовый путь: диспетчеризация статики в cmd/l2go
(build-tag split), Makefile-цели run-*, CI-шаги, инструкция-в-журнал. Конкурентности,
таймингов, рёбер транспорта не добавляется. Уровни — docs/testing.md §3; CLI-шов
`run(args)` — интеграционный по прецеденту P3.12.

| # | Кейс | Уровень | Трассировка | Сценарий | Оракул | TDD-порядок | Приоритет |
|---|---|---|---|---|---|---|---|
| S1 | TestLoadStaticDevRequiresPath | компонентный | S7-п.1 «dev: пустой → ошибка»; корнер «пустой вход» | dev; loadStatic("") | именованная ошибка «артефакт … обязателен», не мусорный текст mmap | красный = компиляция (нет loadStatic); мутация «пустой → LoadFile("")» краснит | ПБ |
| S2 | TestLoadStaticDevLoadsFile | компонентный | S7-п.1 «непустой → LoadFile в обеих сборках» | dev; loadStatic(synth) | meta равна artifact.LoadFile(synth).Meta; err nil | compile-red; мутация «игнорирует путь» → meta ≠ | В |
| S3 | TestLoadStaticDevMissingFileKeepsPathContext | компонентный | codestyle §4 (%w-контекст); деградация «файл недоступен» | dev; loadStatic("/нет/такого.l2a") | ошибка содержит переданный путь | compile-red | В |
| S4 | TestLoadStaticEmbeddedEmptyUsesEmbeddedBytes | компонентный, `//go:build embedded` | S7-п.1 «embedded: пустой → вшитая статика»; критерий самодостаточного бинарника | embedded; loadStatic("") | err nil; meta == LoadFile(internal/artifact/embedded/artifact.l2a).Meta | локально: l2data build синт-артефакта → go test -tags embedded; compile-red до static_embedded.go | ПБ |
| S5 | TestLoadStaticEmbeddedNonEmptyPrefersFile | компонентный, embedded; корнер «источник-самозванец» | S7-п.1 «непустой → файл в обеих сборках» | embedded; loadStatic(артефакт-без-гео, Regions=0) | meta == LoadFile(без-гео).Meta, Regions ≠ вшитому | мутация «всегда LoadEmbedded» краснит | ПБ |
| S6 | TestReplayRequiresArtifactInEmbeddedBuild | интеграционный (run), embedded; корнер «подменённый вход» | S7-п.1 «-replay — строго явный -artifact» | embedded; run(["-replay", пустой t.TempDir()]) без -artifact | ошибка «-replay требует -artifact» (guard до чтения каталога) | мутация «replay через диспатч» даёт «сессия не найдена» → красный | ПБ |
| S7 | CI make-guard run-целей | CI-шаг (make) | S7-п.2 «отсутствие bin/ — ошибка с подсказкой» | checkout без bin/; make run-game / run-login | exit ≠ 0; stderr содержит подсказку «make embedded DATA=…» | красный = шаг против Makefile без guard-строки (свидетельство в «Раундах») | ПБ |
| S8 | make -n печать рецептов + ревью дефолтов | ревью/CI-текст | S7-п.2 «переменные, дефолты = дефолты флагов» | make -n run-game … | печать команды с переменными; сверка дефолтов с FlagSet | процессный (носитель — ревью S8 + фаза B) | В |
| S9 | TestBootstrapEmbeddedEmptyArtifactFullContour | e2e, embedded | спека «make embedded → оба бинарника» сквозной | startE2E с ArtifactPath:"" | слиток входа на вшитой статике, NPC развёрнуты | после S4/S5; дубль-страховка контура | Ж |

Не-кейс, а регрессионная сеть: весь существующий сюит cmd/l2go (e2e + replay) зелёным
после рефакторинга — покрытие «поведение dev-ветки не изменилось» (ворота).

Трассировка «критерий приёмки → носитель»: КА-1 (чек-лист владельцем) и КА-2 (баг =
тест), КА-3 (живые фикстуры) — фаза B, фиктивных кейсов не создаётся, перечень
regression-тестов ведётся в реестре по мере находок; КА-4 (ворота) — S7 покомпонентно
+ CI make check. Корнер-чеклист эксплуатации: границы-0 → S1/S4/S6; валидный файл →
S2/S5; самозванец → S5; деградация «файл недоступен» → S3; интерливинги/тайминги/
нагрузочные пики — не применимы (синхронный стартовый путь, новых горутин/дедлайнов
нет); чурн/рестарты — КТ-5 фаза B; злые входы декодера/mmap-слоя — не дублируем
(пакет artifact, P3.12 evil-args). Анти-петля: P3.4-F7 «молчаливый ноль» → громкий
guard S7-целей; P3.12-F14 «статусы без тестов» → статусы ниже честные;
P3.12-F26 «in-band сигнал» → loadStatic возвращает error, nil-статика запрещена.

Анти-план: парсинг флагов как таковой; интерфейс/func-var «для мока» загрузчика
(codestyle §2 — build-tag split и есть шов); дубли evil-входов декодера артефакта,
replay-CLI злых аргументов dev-ветки (P3.12), гео-лукапов на embedded-байтах,
идемпотентности Build, «make embedded» как таковой (CI embed-шаг уже исполняет);
полный e2e-сьют под тегом в CI (дорого, дубль dev-прогона — точечной группы
достаточно); журнал-инструкция — ревью записи, не автоматика.

Швы (обязательство S7): диспатчер двумя файлами по тегам с общей сигнатурой;
replayRun НЕ переводится на диспатч; CI embed-шаг расширяется tag-группой
`go test -tags embedded ./cmd/l2go -run 'TestLoadStatic|TestReplayRequires'`;
Makefile guard `test -x bin/…` → exit 2 + подсказка; e2eOpts-опция пустого
ArtifactPath — только для S9(Ж); часы/тикер/каналы-оракулы — не заводить.

Статусы кейсов: S1–S6, S7 — планировались → S7 ведёт; S8 — процессный (ревью);
S9 — планировался (Ж).

| F5 | S8 | безопасник | Ошибка загрузки статики потеряла путь/метку источника: bootstrap-обёртка «l2go: статика: %w» — decode-ошибки существующего файла (split/DecodeError внутри artifact) пути не несут; в embedded-сборке отказы вшитых байт и внешнего файла неразличимы — регресс против прежнего «артефакт %s: %w» | main.go (диф S7): старое «l2go: артефакт %s: %w» → «l2go: статика: %w»; internal/artifact: ошибки decode без пути; сценарий: -artifact /tmp/старый.l2a другой версии → «версия …» без имени файла | минор | закрыта (правкой) | Диспетчер несёт источник: dev/embedded непустой путь — «артефакт %s: %w», embedded пустой — «вшитые байты бинараря: %w»; bootstrap — префикс «l2go: %w» (текст dev-guard побитово прежний) |
| F6 | S8 | безопасник | Журнал-инструкция п.7 читается «любые производные байты тапа — можно в testdata»; кодный фильтр фикстур исключает только AuthLogin — ключи сессии и живой контент остаются в читаемом логе | план/прогресс.md п.6–7 против doc cmd/l2tap («журнал и читаемый лог — секрет») и internal/tap/decode.go:328; сценарий: -out-log traffic.txt → коммит в testdata с ключами | минор | закрыта (правкой) | П.7 дополнен: в testdata — только фильтрованные протокольные фикстуры (-out-fixtures); читаемые логи/журналы — секрет в var/ |
| F7 | S8 | тест-практик | S9: заявленный оракул «NPC развёрнуты» не ассертится (только слиток) — население неразвёрнуто оставляет тест зелёным | static_embedded_test.go S9 против плана кейса; прецедент TestE2ENpcInfoRadiusOnly ждёт NPC_INFO-кадры | мажор | закрыта (правкой) | waitForLine(…, "NPC_INFO", 3s) после слитка |
| F8 | S8 | тест-практик | S5: предусловие фальсификатора (вшитый артефакт несёт гео) проверяется совмещённым ассертом → ложный красный на легитимной сборке без гео | static_embedded_test.go:69–73: при Regions=0 у вшитого корректный код даёт «подмена источника» | минор | закрыта (правкой) | Отдельный guard предусловия: t.Skipf с причиной «фальсификатор неприменим» |
| F9 | S8 | тест-практик | Близнецы S2/S3 против S4/S5 несимметричны по t.Parallel (I/O-профиль один) | static_dev_test.go против static_embedded_test.go | минор | закрыта (правкой) | t.Parallel оставлен только чистому юниту S1; S2/S3 — без (как S4/S5) |
| F10 | S8 | тест-практик | Разные идиомы сравнения Meta в близнецах (*meta != *want vs reflect.DeepEqual + импорт reflect) | static_dev_test.go:42 против static_embedded_test.go:36,63 | минор | закрыта (правкой) | Прямое сравнение *meta != *want в обоих; reflect убран |
| F11 | S8 | тест-практик | Уровень S9 заявлен «e2e», фактически — интеграционный (сервер и клиент в одном процессе) | startE2EOpts — однопроцессный контур; docs/testing.md §3 | минор | закрыта (правкой) | Терминология реестра/комментария: интеграционный, имя по конвенции пакета TestE2E* |
| F12 | S8 | тест-практик | embedded-группа не гоняется под -race нигде (make check тег не включает; CI-шаг без -race) | ci.yml:57; S9 — полный контур с горутинами | минор | закрыта (правкой) | Оговорка в CI-комментарии и реестре: race-эквивалент — dev-прогон make check; новой конкуренции нет (стартовый путь), группа — точечная компиляционно-поведенческая |
| F13 | S8 | тест-практик | Нет свидетельства -count=2 для флаки-чувствительной embedded-группы | docs/testing.md §0; реестр S7 — только одиночный прогон | минор | закрыта (правкой) | Локально: embedded-группа -count=2 → 8/8 PASS; race-прогон пакета зелёный (раунд ниже) |
| F14 | S8 | тест-практик | Нит: опечатка в комментарии S5 («у вшитого — нет» вместо «есть») | static_embedded_test.go:42 | минор | закрыта (правкой) | Исправлено |

## Отклонённые (память анти-петли)

| ID | Утверждение | Причина отклонения | Раунд |
|---|---|---|---|

## Исправления

| ID | Правка | Аргументация (альтернативы и почему хуже) | Чем проверяется закрытие |
|---|---|---|---|

## Раунды

- 2026-09-19 · S0/S1 · ведущая сессия · Самопроверка плана — чисто. Проработка объёма
  фазы A (подготовка; фаза B — живой прогон владельца по чек-листу КТ-5):
  1. **Разрыв, найденный инвентаризацией:** `make embedded` собирает `bin/`, но
     `artifact.LoadEmbedded` никем не вызывается — `bin/l2go` даже embedded-сборки
     требует внешний `-artifact` (cmd/l2go/main.go:124,133,374). Спека «make embedded →
     оба бинарника» как подготовка запуска требует самодостаточных бинарников → S7
     подключает LoadEmbedded за build-тегом: пустой `-artifact` в embedded-сборке грузит
     вшитую статику; в dev-сборке пустой `-artifact` — ошибка (как сейчас); непустой —
     LoadFile (mmap) в обеих сборках. Dev-вариант с mmap-артефактом — флаг `-artifact`
     (уже существует). Режим `-replay` остаётся строго с явным `-artifact` (тот же
     артефакт, которым записана сессия).
  2. Makefile-цели `run-login`/`run-game`: запуск `bin/`-бинарников (embedded-конфигурация)
     с параметрами через make-переменные (дефолты = дефолты флагов); отсутствие `bin/` —
     ошибка с подсказкой «сначала make embedded DATA=… GEO=…».
  3. Инструкция запуска и конфиг (мир: -hz/-addr/-link/-tls; спавн-зона: -npc-center/
     -npc-radius; каталоги персиста/порций; -portions-payloads для реплея сессии) —
     в журнал план/прогресс.md, без нового файла; туда же чек-лист КТ-5 и правило
     секретов/фикстур (прецедент P1.6, doc cmd/l2tap).
  4. Фаза B (после прогона владельца): баг = фикс + regression (реплей или
     интеграционный тест); живые фикстуры — производные байты в testdata; выжимка чисел
     (счётчики сущностей, NpcInfo на вход, join-доля, время старта) в журнал.
  Чеклист S1: инварианты AGENTS.md — код-изменение локально в cmd/l2go + Makefile, новых
  горутин/мутаций/рёбер транспорта нет; матрица ADR-0005 — новых пакетов/рёбер нет
  (cmd/l2go → internal/artifact существует), checkdeps зелёный; план тестов — S6b далее
  (обязательная ступень до S7); критерии фальсифицируемы (чек-лист КТ-5 бинарный по
  пунктам, ворота, «баг = тест»); лицензии — артефакт/var/bin вне репо (.gitignore),
  фикстуры только производные байты. Ворота прогнаны покомпонентно до старта: зелёные
  (gofmt/vet/build/staticcheck/checkdeps/test -race; make на машине отсутствует).
- 2026-09-19 · S6b · тест-планировщик · Тест-план составлен (раздел «Тест-план»):
  9 позиций (S1–S9), из них 5 приёмка-блокеров; находки на спеку F1–F4 (F1 мажор —
  правка CI включается в объём S7). Корнер-чеклист пройден явно, анти-план и швы
  зафиксированы. Красный для CI make-guard (S7-кейс): прогон шага против Makefile
  до добавления guard-строк — свидетельство ниже в раунде S7.
- 2026-09-19 · S7 · ведущая сессия · Реализация фазы A. Код: cmd/l2go/static_dev.go
  + static_embedded.go (диспетчер loadStatic по build-тегу; bootstrap переключён),
  e2eOpts.useEmbeddedStatic (шов S9), Makefile run-login/run-game (guard
  `test -x bin/…` → exit 2 + подсказка в stderr; run-game — `-portions-payloads`),
  ci.yml (tag-группа в embed-шаге + шаг make-guard), инструкция-конфиг в
  план/прогресс.md. TDD-свидетельства: dev-группа — красный (undefined: loadStatic)
  → зелёная; embedded-группа — красный (undefined) → зелёная (локальный
  синт-артефакт тем же конвейером, что CI: synth + golden_flat/16_10 +
  golden_ml/17_10); мутационные пробы: «embedded игнорирует путь» краснит S5
  (Regions 2≠0), «replay через диспатч» краснит S6 (guard-текст потерян, пришёл
  «сессия не найдена»), «dev guard недостижим» краснит S1 (мусор «open : no such
  file» вместо именованного текста). Make-guard: красная сторона локально
  эмулирована фрагментом рецепта без guard-строки (rc=127, подсказки нет → grep
  шага красит), зелёная — с guard (rc=2, подсказка в stderr); полный шаг — CI
  (make локально отсутствует — оговорка). Отпечаток реплей-фикстуры не дрейфнул:
  локальная пересборка артефакта бит-в-бит совпала с artifact.sha256 (SHA-MATCH).
  Статусы кейсов: S1–S6, S9 — реализованы; S7 — реализован (локально фрагментно,
  носитель полного прогона — CI); S8 — процессный (закрывается в S8). F1–F4 —
  закрыты правкой.
- 2026-09-19 · S8 раунд 1 · безопасник + тест-практик (параллельно, слепо) ·
  Безопасник: 0 блокеров/0 мажоров/2 минора — гигиена данных чиста (диф
  текстовый, artifact.l2a/var/bin вне индекса по .gitignore, CI строит артефакт
  только из синт-фикстур), матрица источников диспатча чиста по всем четырём
  клеткам, сервер в CI не запускается, make-guard ловит снос guard-строки;
  F5 (метка источника в ошибке), F6 (граница секретов в инструкции п.7).
  Тест-практик: трассировка план→тесты полная (приёмка-блокеры S1/S4/S5/S6/S7 —
  все с носителями, сирот нет, швы исполнены), мутационные пробы валидны,
  снов/фиксированных портов нет; 1 мажор F7 (S9 не ассертит NPC), миноры
  F8–F14. Правки раунда 1: F5–F14 закрыты (диспетчер несёт источник ошибки,
  bootstrap-префикс «l2go: %w» — dev-guard-текст побитово прежний; п.7
  инструкции дополнен границей секретов; S9 ждёт NPC_INFO; guard предусловия
  S5 через t.Skipf с причиной; t.Parallel только S1; единая идиома сравнения
  Meta; терминология S9 — интеграционный; CI-комментарий про race-эквивалент).
  Прогоны: dev-группа -count=2 зелёная; embedded-группа -count=2 → 8/8 PASS;
  gofmt/vet (оба тега)/staticcheck чисто; пакет cmd/l2go под -race зелёный.
  Выход из цикла: ноль подтверждённых блокеров/мажоров (бюджет ревью
  реализации 1/3). Замыкание — отдельным раундом ниже.
- 2026-09-19 · S8 замыкание · безопасник + тест-практик · Оба автора
  подтвердили адресацию всех своих находок по дельте 20bd8b9 (F5–F14),
  остатков нет; независимая верификация практиком на этой машине: dev-группа
  3/3, embedded-группа 3/3 (S5 активен), S9 PASS с NPC_INFO-ассертом,
  gofmt чист; побочно подтверждено, что параллельная правка F5 оракулы
  кейсов не ломает (dev-guard-текст побитово прежний, replayRun не тронут).
  Нулевая заметка замыкания: опечатка «стартальной» в комментарии S9 —
  исправлена тем же касанием. Финальные ворота покомпонентно: зелёные
  (gofmt/vet/build/staticcheck/checkdeps/test -race, exit=0; make на машине
  отсутствует — CI). S8 закрыт: конвейер фазы A завершён, фаза B (живой
  прогон владельца по чек-листу КТ-5) — точка владельца.
