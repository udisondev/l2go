# Реестр находок P3.2

- Задача: P3.2 «Метроном, регион-актор, свёртка и лог порций» (план/p3.md)
- Ревью-профиль: S2 — инвариантщик, конкурентщик, перформансер, прагматик; S8 — +
  тестировщик
- Бюджеты: план 1/2 · рестарты 0/2 · ревью реализации 0/3

## Находки

| ID | Стадия | Роль | Утверждение | Свидетельство | Серьёзность | Статус | Решение |
|---|---|---|---|---|---|---|---|
| F1 | S2 | конкурентщик + инвариантщик | номер шага из cap-1 канала устаревает: при дропе тика регион читает буферизованный старый номер (LastDelta=2 недостижим, D1-семантика «дроп → dt=2×периода» ломается); немедленный шаг с Now() обгоняет буферизованный токен → step(n<lastStep): dt<0, регресс Beat/doneTick | трасса: такты 10–12, send 11 буферизован, 12 дроп → шаг с 11: delta=1; notify-шаг с Now()=7 → канал отдаёт 6 → delta=−1 | блокер | подтверждена | тик-канал — дверной звонок struct{}; номер шага = Metronome.Now() на старте шага; звонок сбрасывается перечитом; Now() монотонен ⇒ n ≥ lastStep всегда |
| F2 | S2 | инвариантщик | пустые и dt=0 шаги не пишутся в лог → реплей не воспроизведёт Steps/Noise/Beat (D6 бит-в-бит недостижим); xor-Noise самопогашается на повторном шаге тика | spurious-токен/тик без писем → шаг без записи в логе; два шага в тике n: PCG-сид совпадает → x^x=0 | мажор | подтверждена | запись границы шага — на каждый шаг (пустые и dt=0 включительно); Noise — аккумулятор wrapping-add (не xor) |
| F3 | S2 | конкурентщик + инвариантщик | паника посреди применения изъятой пачки: остаток изъят (знак двинут), не применён, не класс-дропнут — тихая потеря reliable (EnterWorld исчезает без метрики) | ADR-0003 §7 «≠0 финальных дропов reliable — инцидент», §4 недообработанная пачка; прецедент P3.1 DropBatch | мажор | подтверждена | recover → DropBatch неприменённого остатка (классовый дроп + slog); запись порции в лог — после успешного применения |
| F4 | S2 | конкурентщик | спящий (деактивированный) регион теряет страховку метронома от остаточного окна F36 (письмо в очереди без токена) → контрольное письмо спит вечно | F36 P3.1: лицензированная защита «метрономом»; деактивированный тиков не получает; зеркально обязательству P3.3 тик-фолбэк | мажор | подтверждена | heartbeat-фолбэк метронома: каждый H-й тик non-blocking звонок всем регионам (второй сет); регион на фолбэк — перечит контрольного ящика (пусто ⇒ ничего, непусто ⇒ немедленный шаг) |
| F5 | S2 | конкурентщик | dt после реактивации из сна не специфицирован: lastStep только «при старте» → пробуждение на тике 90000 даёт dt≈часы; LastDelta — мусор, P3.9 унаследует | сценарий: пусто на тике 100 → EnterWorld на 90000 | мажор | подтверждена | шаг после сна исполняется с delta=0 (симуляционное время региона не тело во сне); поле slept в горутине региона |
| F6 | S2 | конкурентщик | Close/flush лога не привязан к выходу Run по ctx; Close извне — гонка с финальным шагом | хвост bufio (~4 КиБ) теряется → реплей расходится с прод-дампом | мажор | подтверждена | лог закрывает сама горутина региона при выходе из Run; внешний Close запрещён контрактом |
| F7 | S2 | перформансер | бенчмарк тика не задаёт профиль: дрен и лог порций выпадают из baseline; нет аллокационного бюджета | пустые ящики → step вырождается в фазу A; perf.md §0 «без baseline запрещено»; реестр ADR-0005 включает дрен в путь тика | мажор | подтверждена | под-кейсы Idle / Loaded-1 / Loaded-10 (+ контрольная смесь), лог payloads=false/true (tmpdir); машинный бюджет: 0 аллок на Idle-шаг вне роста пачек/снапшота/RNG |
| F8 | S2 | перформансер | буферы пачек дрена не заданы: наивный Extract — до N слайс-аллокаций на тик; порядок «лог до переиспользования» не проговорен | mailbox.go ExtractInto существует ради этого; perf.md §1 | мажор | подтверждена | буферы (контрольный, дрен, общий список) — поля Region, переиспользуются; содержимое пачки живёт до записи лога |
| F9 | S2 | перформансер | сериализация записи порции без дисциплины аллокаций и вне бенчмарка | наивный энкодер — тысячи аллокаций/тик при 4096 письмах | мажор | подтверждена | кодирование — append в переиспользуемый буфер лога; 0 аллок на запись вне роста — машинный тест; в бенчмарке (через F7) |
| F10 | S2 | прагматик | сигнатура Fold: типы portions/advisory не определены; residents непубличен — внешний реплей не вызовет; дублирование tick/Portion.Tick | P3.12 — внешний потребитель шва; world.Portion уже несёт Tick | мажор | подтверждена | Fold(tick, delta, rng, st, ents []*Entity, portions []Portion, adv []AdvisoryIn): ents — сортированный слайс сущностей (актор — проекция residents; реплей — из дампа); AdvisoryIn — world-тип {Cell, Entity} без импорта replica в фазе 3 |
| F11 | S2 | прагматик | критерий «кап фазы B переносит излишек» нефальсифицируем: fold фазы 3 возвращает пусто — производителя outbox нет | противоречие «критерии фальсифицируемы» | мажор | подтверждана | юнит-тест механики outbox в пакете world: синтетические конверты → capped-флаш переносит излишек |
| F12 | S2 | инвариантщик | обязательство P3.1/F49 (потребитель водяного знака — заголовки порций D5) не перенесено: mark остаётся write-only | реестр P3.1 F49; mailbox.go:66 | минор | подтверждена | заголовок пачки лога несёт id ящика и его водяной знак; transport получает accessor Mark() (минимальная правка реестра) |
| F13 | S2 | инвариантщик | читательский протокол P3.1 (AckNotify + перечит после сброса) для контрольного ящика не перенесён в спеку | F36 P3.1 — слой защиты; mailbox.go:249–251 | минор | подтверждена | пункт спеки: после дрена — AckNotify и перечит; критерий — письмо в окне дрена ⇒ следующий шаг виден |
| F14 | S2 | инвариантщик + конкурентщик + прагматик | взаимодействие K и drainBudget не зафиксировано: счёт контрольных, граница бюджета против неделимой пачки (два расходящихся прочтения), слово «разреживается» ложен | 20 при K=16 и бюджете: реализатор может гейтить общий список → частичное применение пачки | минор | подтверждена | контрольные (K + излишек) применяются всегда, в бюджет не входят; бюджет воротит только обход сущностных ящиков; правило границы: остаток ≥1 ⇒ пачка целиком (перерасход метрится); K упорядочивает (приоритет владения), не ограничивает — шторм контрольных = полный разбор шага, защита вотчдог |
| F15 | S2 | конкурентщик | флаг «эпизод лага» — место хранения не задано: в полях Region метроном пишет в состояние региона из чужой горутины; деактивация в лаге не сбрасывает эпизод | дисциплина владения; сценарий залипания | минор | подтверждена | состояние эпизодов — собственность горутины метронома (ключ — регион); сброс при деактивации; в Region — только атомарный doneTick |
| F16 | S2 | конкурентщик | порядок backlog фазы B против свежих писем не закреплён: переупорядочение исходящих одного получателя | И5-аналог; потребитель — per-client FIFO P3.6 | минор | подтверждена | сначала backlog, затем свежие письма шага |
| F17 | S2 | перформансер | ранний стоп обхода дрена — систематическое голодание хвоста id при стойком перегрузе | шторм FAF в младшие id ⇒ старшие не изымаются никогда; И5 порядок между ящиками не определён | минор | подтверждена | кольцевой старт обхода — детерминированная функция тика (tick mod len); полный оборот за шаг |
| F18 | S2 | перформансер | []*resident: довод «слайс значений невозможен (карта держит &box навечно)» не записан; мостик к SoA-проходу P3.9 не проговорен | registry.go хранит *Mailbox навечно; реестр зовёт путь «SoA-проход» | минор | подтверждена | предложение в спеку + строка о происхождении SoA-прохода из residents (P3.9) |
| F19 | S2 | перформансер | baseline не наследует дисциплину прецедента P3.1 (benchstat, -count, окружение) | benchmarks/P3.1-transport-baseline.txt; perf.md §0 | минор | подтверждена | benchstat-совместимый прогон -count=10, шапка окружения, машина зафиксирована |
| F20 | S2 | перформансер | outbox фазы B — очередь без декларации (предел/политика/метрики) | ADR-0003 §4 единая декларация очереди | минор | подтверждена | декларация: предел отсутствует (исходящие региона — надёжный класс, живому не дропаются), глубина — метрика + алерт, троттлинг compose — фаза 4 |
| F21 | S2 | прагматик | дефолты phaseBCap и freezePanics без аргумента | раздел требует «дефолты с аргументом» | минор | подтверждена | аргументы добавлены (стартовая калибровка, тюнинг — фаза 4) |
| F22 | S2 | прагматик | валидация конфига не специфицирована: time.Second/Hz при Hz≤0 — паника на недоверенном входе | codestyle §5: недоверенный вход — только ошибка | минор | подтверждена | New возвращает ошибку: Hz>0, K/бюджеты/капы >0, dir при включённом логе |
| F23 | S2 | прагматик | «порядок фаз наблюдаем (счётчики)» — счётчики не различают порядок (после N шагов все = N при любой перестановке) | фальсифицируемость | минор | подтверждена | критерий уточнён: все фазы по разу на шаг + паника в фазе B ⇒ A-счётчик вырос, B/publish/ack — нет |
| F24 | S2 | прагматик | ротация: seq без стратегии на рестарте — усечение/смешение сессий | kill -9 → рестарт открывает тот же seq | минор | подтверждена | seq = max существующих в каталоге + 1 при старте писателя |
| F25 | S2 | прагматик | запись «переезды» в формате лога — поле без производителя до фазы 4 (анти-скоуп фазы) | YAGNI codestyle §2; формат версионируется | минор | подтверждена | исключено из P3.2; добавится версией формата вместе с хэндоффами (фаза 4) — отклонение от буквы фазового плана с аргументом, D5 исполняется фазой 4 |
| F26 | S6 | свежий взгляд №2 + №3 | запись границы не несёт delta: пары тиков неотличимы («проснулся delta=0» vs «пережил дропы delta=4») — реплей обязан вычислять delta одинаково, из номера невозможно; LastDelta из Dump невосстановим | трасса: сон на тике 100 → пробуждение на 600, реплей из разницы получит 500; критерии «LastDelta=2 при дропе» и «=0 после сна» внутренне противоречивы | мажор | подтверждена | delta (varint, тики) в запись границы; период метронома в заголовок файла (P3.9 переводит тики во время) |
| F27 | S6 | свежий взгляд №2 | политика I/O лога не задана: ошибка записи = молчаливая дыра D5; оборванный хвост (ENOSPC/power-loss) неотличим от валидных данных ридером | сценарий: ENOSPC → шаги продолжаются, реплей теряет входы без следа; частичная запись без фрейминга — невоспроизводимый падения replay-гейта | мажор | подтверждена | ошибка записи → заморозка + slog-алерт (общий механизм); каждая запись — фрейм {длина, crc32, тело} |
| F28 | S6 | свежий взгляд №2 + №1 | паник-шаг не попадает в лог — реплей-достоверность обрывается тихо, расхождение D6 необъяснимо из носителя | recover-политика + «запись после применения» ⇒ после паники лог невоспроизводим без следа причины | мажор | подтверждена | маркер сбойного шага {tick, фаза} пишется в recover; ридер репортит обрыв |
| F29 | S6 | свежий взгляд №3 | fold не владеет рождениями/удалениями: применить EnterWorld в чистой fold нельзя (ents — проекция); вызов Remove из применения пачки — удаление из-под обхода дрена; реплей не воспроизводит население | сценарий: удаление сдвигом из-под итерации → пропуск ящика/паника; Dump сущностей невоспроизводим | мажор | подтверждена | StepResult{Out, Births, Retires}: эффекты населения вычисляются fold, применяются актором после обхода дрена (Register+Claim/вставка; Despawn→Retire/удаление); реплей — той же чистой частью |
| F30 | S6 | свежий взгляд №3 | EntityID рождений недетерминирован относительно лога (сквозной счётчик реестра, порядок регистраций шлюза/persist/региона) — бит-в-бит P3.12 держится на совпадении порядка Register | registry.go:56; EnterWorld адресован региону, присвоенного ID в логе нет | мажор | подтверждена | записи рождений/удалений несут присвоенные EntityID — работает при любом порядке регистраций, переживёт мультирегион |
| F31 | S6 | свежий взгляд №3 | источник тика шлюза не определён: метроном звонит только регионам; свой Ticker в P3.6 — второй pacing с фазовым дрейфом | P3.6 «на каждом тике собирает кадры» против «звонок — только регионы» | мажор | подтверждена | подписка сервисных адресатов на звонок метронома (метод; общий механизм, не отдельная ветка) |
| F32 | S6 | свежий взгляд №1 | место применения излишка контрольных не задано — два реализатора дадут разные дампы | порядок общего списка против сущностных пачек не специфицирован | минор | подтверждена | излишек применяется после обхода сущностных ящиков |
| F33 | S6 | свежий взгляд №2 | поведение активного региона на фолбэк не специфицировано | два корректных прочтения (игнор/шаг delta=0) — правило должно быть одно | минор | подтверждена | активный игнорирует фолбэк (окно закрыто тик-звонком) |
| F34 | S6 | свежий взгляд №2 | time.Second/Hz — целочисленное деление: Hz=15 даёт 66 мс (фактически 15.15 Гц) | арифметика | минор | подтверждена | период = time.Duration(float64(time.Second)/float64(hz)) |
| F35 | S6 | свежий взгляд №2 | паника Close лога уронит выход по ctx без журнала | сценарий | минор | подтверждена | закрытие обёрнуто recover со slog |
| F36 | S6 | свежий взгляд №3 | порядок Remove (Despawn → Retire → освобождение) не перенесён в спеку — P3.7 соберёт ExitWorld из этих вызовов | контракт P3.1 mailbox.go | минор | подтверждена | порядок зафиксирован в пункте эффектов населения |
| F37 | S6 | свежий взгляд №3 | ридер цепочки файлов по seq не зафиксирован | P3.12 читает сессию целиком | минор | подтверждена | критерий раундтрипа дополнен цепочкой по seq |
| F38 | S6 | свежий взгляд №2 | кольцевой старт: латентность O(len−B) на ящик при устойчивом исчерпании бюджета (оборот полный, но медленный: len=10000, B=4096 → до ~10 мин) | числа: окно съезжает на 1 за тик | минор | подтверждена | реестр остаточных рисков; carry-старт — фаза 4 по измерениям (реплей нейтрален: дрен не реплеится) |
| F39 | S8 | инвариантщик + конкурентщик + прагматик + тестировщик | RegionStats.Backlog — data race: len(r.outbox) из чужой горутины при записи слайса каждым шагом; -race ворота CI красные детерминированно; комментарий «гонка допустима» легализует нарушение инварианта 3 | region.go:143 + трассы поллинга Stats при живом Run (два теста-свидетеля) | блокер | подтверждена | backlogLen atomic.Int64 (обновление в phaseB, чтение в Stats); комментарий снят. Раунд-2: фикс применился наполовину (atomic был write-only, Stats читал слайс) — переоткрыт 5 ролями, закрыт в раунде-3 (микродофикс) |
| F40 | S8 | 4 роли (воспроизведено прогоном) | recover не дропает волну перечита AckNotify (rereadBuf/restBuf): письмо изъято, не применено, не класс-дропнуто — тихая потеря reliable (F3/ADR-0003 §7) | проба: EnterWorld в окне дрена + паника fold → FinalReliable=0, mark=1, depth=0 | мажор | подтверждена | DropBatch(restBuf); гейт по фазе: дроп только при панике в drain/fold (поздние фазы письма уже применили — ложных reliable-инцидентов нет); тест TestRegionPanicDropsRereadWave |
| F41 | S8 | 5 ролей (воспроизведено) | кэш entsProj инвалидации по длине: равные Births+Retires шага оставляют fold на мёртвой проекции (Beat удалённой, новорождённая без симуляции) — D6-раундтрип ломается с первым производителем эффектов (P3.7/P3.10) | region.go entsProj len-детектор; проба birth+retire | мажор | подтверждена | поколение населения popVersion (инкремент Spawn/Remove), кэш по поколению; тест TestRegionPopulationCompensatingStep |
| F42 | S8 | тестировщик | паника ридера лога на злом uvarint: счётчик ≥2^63 → отрицательный int → make(0, отрицательное) — паника вместо ошибки на недоверенном входе (порченый/вредоносный лог) | portionlog parseStep/parseEntity проверки nb<=len(body) пропускают отрицательные | мажор | подтверждена | беззнаковые сравнения (> uint64(len)) во всех счётчиках |
| F43 | S8 | перформансер (memprofile + -gcflags=-m) | writeFrame аллоцирует на каждом шаге (hdr → moved to heap): 4-я аллокация Idle вопреки декларации «0 аллок вне роста»; zero-alloc-тест покрывал только энкодер | memprofile: 211 объектов на 212 вызовов; escape: portionlog.go:215 | мажор | подтверждена | префикс кадра — поле писателя l.prefix (append), escape чист; Idle 4→2 аллокации; бюджет уточнён до ровно 2 |
| F44 | S8 | прагматик + тестировщик + конкурентщик | флаки-race в тесте: чтение state.KindCounts до cancel/wg.Wait при живом регионе | region_test.go мёртвый блок с ложным комментарием | мажор | подтверждена | блок удалён, ассерт после wg.Wait |
| F45 | S8 | прагматик + тестировщик + конкурентщик | тест кольцевого старта не фальсифицирован: `_ = start`, ассерт истинен при любом порядке; при tick=0 кольцо совпадает с головой | region_test.go | мажор | подтверждена | tick=7 (mod 3=1), сверка полного порядка пачек с rot(residents,1) |
| F46 | S8 | инвариантщик + перформансер + тестировщик | бенчмарк без контрольной смеси: приоритетная ветвь K и общий список не измерены (F7) | deliverLoad шлёт только KindAggro | мажор | подтверждена | смесь: каждое 16-е письмо контрольное; baseline рев.2 |
| F47 | S8 | инвариантщик + прагматик + тестировщик | критерий F27 «ошибка записи → заморозка» без теста | ветка freeze не исполнялась | мажор | подтверждена | TestRegionFreezeOnLogWriteError: закрытый файл + волна 2048 контрольных → кадр > bufio → EBADF → frozen |
| F48–F64 | S8 | панель (миноры пачкой) | маркер паники нёс Now() вместо тика шага; adviseBuf не чистился в recover; перерасход drainBudget не метрился; AppliedBirth.Ent.ID=0 (реплей ≠ прод); openFile игнорировал ошибку заголовка; пустой файл kill-9 ломал чтение цепочки; ротация при кадре > maxFile — «файл на кадр»; регион при выходе не покидал сеты метронома; watchdog — map-аллокация на каждом такте; subscriber-обёртка; пустые литералы; Ph*-обрезание; frameStart-мусор; depthTotal в прод-файле; дублирование разбора имён; два источника дефолта 64 МиБ | см. отчёты ролей | минор | подтверждены | все закрыты: stepTick; чистка adviseBuf; drainOverrun-метрика; Ent.ID присваивается; header err; пустые файлы пропускаются; негабаритный кадр после ротации без повторной; shutdown() покидает сеты; watchdog без аллокаций ([]активные линейный поиск); []chan struct{}; nil-слайсы; Phase*; удалено; хелпер в тесте; listSeqFiles; defaultMaxFileBytes один источник |
| F65 | S8 | тестировщик | dropped-метрика и «контрольные вне drainBudget» не фальсифицированы; стресс не пробивал FAF-кап жителя; сброс серии успехом не ассертится; снапшот-тик не ассертится; Subscribe-хвост без утверждения | отчёт | минор | подтверждена | TestMetronomeDroppedMetric; CtrlBudget c DrainBudget=5; FAF-волна 2048 в один ящик + сбор BoxStats жителей; ассерты серии/снапшота/Subscribe добавлены |
| F66 | S8 | тестировщик | baseline: CJK-мусор, опечатка, перегрузная маркировка только 1 из 3 кейсов | benchmarks файл | минор | подтверждена | текст чищен; перегрузная оговорка на все 3 кейса; рев.2 со смесью |

| F67 | S8-р2 | инвариантщик + конкурентщик + прагматик (проба) | двойной классовый дроп излишка K: restBuf содержит копии ctrlBatch (surplus ∪ reread), recovered дропает оба → письма сверх K считаются дважды (проба: 20 reliable → FinalReliable=24); окно паники phaseDrain до сброса буферов дропает пачки прошлого шага | region.go recovered; проба раунда-2 | мажор | подтверждена | дизъюнктный покров: DropBatch(ctrlBatch) + DropBatch(rereadBuf), restBuf не дропается; resetDrain() на старте шага — паника дрена не задевает прошлый шаг; тест TestRegionPanicDropCoverDisjoint (ровно 20) |
| F68 | S8-р2 | панель (миноры-остатки) | drainOverrun write-only (нет в RegionStats); счётчики ридера вне границ — молчаливый пропуск, не ошибка; ротация негабарита — комментарий без поведения; ридер цепочки: hdr.Region==0 маркер первого файла ломает контроль для региона 0, seq=i+1 врёт при дырах; ассерт сброса серии не фальсифицировал; негативные кейсы конфига неполны; «лог без паник-шага» не ассертится | отчёты раунда-2 | минор | подтверждены | DrainOverrun в RegionStats; счётчики > len(body) — ошибка; ротация: файл из одного заголовка не ротачивается (поведение); first-флаг + fileSeq(path); ассерт «не заморожен до 3-й подряд»; кейсы DrainBudget/PhaseBCap/FreezePanics=0 и LogMaxFileBytes<0; len(steps)==0 в паник-тесте |
| — | S8-р2 | тестировщик | F13 (письмо в окне дрена ⇒ следующий шаг) — прямого регионного теста нет | полисится протоколом транспорта (TestNotifyNoLostWakeup P3.1: enqueue-wake + AckNotify-перезарядка) + немедленным шагом по notify (TestRegionSleepWakeDeltaZero/HeartbeatFallback) | — | зафиксировано | покрытие транспортом P3.1 — достаточное основание; запись здесь закрывает процессную дыру |

### Отклонения диффа от плана (записи-обоснования S8-трассировки)

- `transport.Mailbox.ExtractAppend` — расширение API транспорта за рамки исходного
  плана P3.2: дрен актора накапливает пачки нескольких ящиков в одном буфере с нулём
  аллокаций (F8); ExtractInto сбрасывает буфер и перезаписывал бы пачки соседей
  (обнаружено в S7, воспроизведено зондом). Правка реестра Kind по правилу
  «Kind-расширения рождаются в задаче-потребителе» распространена на API изъятия:
  семантика контракта изъятия/сбора не изменилась (тот же extractInto), тест добавлен
  (TestMailboxExtractAppend). Тестировщик S8 отметил процессную дыру — закрыто этой
  записью.

## Отклонённые (память анти-петли)

| ID | Утверждение | Причина отклонения | Раунд |
|---|---|---|---|
| — | немедленный шаг по контрольному письму — полная цена шага O(население) без коалесинга (1000 EnterWorld → 1000 проходов) | свидетельство опровергнуто: notify-токен cap-1 коалесит волны — все письма, пришедшие во время шага, обрабатываются одним следующим шагом (mailbox.go:100, wake неблокирующий в cap-1); цена одиночного прохода между тиками измеряется бенчмарком F7 и принята r3 §6 (немедленный тик входа); автор отозвал в S5 | 1 |
| — | период метронома в State (свежий взгляд №3, минор) | отклонено: заголовок лога + конфиг покрывают потребителя (P3.9), State — детерминированное состояние свёртки, конфигурация там не нужна | S6 |

## Исправления

| ID | Правка | Аргументация (альтернативы и почему хуже) | Чем проверяется закрытие |
|---|---|---|---|
| F1 | спека: звонок-канал + Now() | альтернатива «номер в канале + clamp max(n, lastStep)» чинит регресс, но не даёт LastDelta=2 после дропа (буфер хранит старый номер) — критерий D1 невыполним | тест дропа: медленный шаг → delta=2; тест монотонности n |
| F2 | спека: запись на каждый шаг; Noise += | альтернатива «логировать только непустые + отдельный счётчик шагов в State» ломает раундтрип (число шагов не восстановимо из лога) | тест: N пустых шагов → N записей; раундтрип сохраняет число шагов |
| F3 | спека: DropBatch в recover; лог после применения | альтернатива «доигрывать остаток следующим шагом» — состояние после паники не специфицируется, доигрывание недетерминированно | тест: паника на k-м письме пачки → финальные дропы = размер остатка, лог полон |
| F4 | спека: heartbeat-фолбэк | альтернатива «не деактивировать никогда» — ломает «пустые регионы стоят ноль»; «TTL на письма» — ожидание в конвейере | тест: спящий регион + письмо без токена → шаг ≤ H тиков |
| F5 | спека: delta=0 после сна | альтернатива «честные часы сна» — мусорный dt, унаследованный движением P3.9 | тест: сон 1000 тиков → пробуждение: LastDelta=0 |
| F6 | спека: Close лога в Run | альтернатива «sync.WaitGroup + внешний Close» — гонка с финальным шагом без выгоды | тест: cancel → файл читается полностью |
| F7 | спека: под-кейсы бенчмарка + аллок-бюджет | альтернатива «один кейс» — дрен и лог не видны в baseline | бенчмарк + машинный аллок-тест Idle |
| F8 | спека: буферы — поля Region | альтернатива «пул» — не аргументирован профилем (perf.md) | аллок-тест Idle |
| F9 | спека: append-энкод в свой буфер | альтернатива «encoding в новый []byte» — аллокации на ровном месте | аллок-тест записи |
| F10 | спека: сигнатура Fold (ents []Entity) | альтернатива «экспорт resident» — раскрывает внутреннее актора; «интерфейс населения» — интерфейс для моков, запрещён | компиляция внешнего вызова Fold в тесте |
| F11 | спека: юнит-тест outbox | альтернатива «перенос критерия в P3.7» — каркас фазы B остаётся непроверенным до P3.7 | тест capped-флаша |
| F12–F25 | спека: соответствующие пункты (см. решения в таблице находок) | — | тесты по критериям приёмки |

## Раунды

- Раунд 1 (S2, 2026-09-14): 4 роли слепо параллельно. Слито и верифицировано: 25 находок
  (1 блокер, 10 мажоров, 14 миноров); 1 отклонена (коалесинг notify опровергнут
  свидетельством, автор отозвал в S5). Бюджеты: план 1/2 · рестарты 0/2. Вход в S4.
- S4 (2026-09-14): спека переписана по F1–F25 (дверной звонок + Now(); запись границы на
  каждый шаг; Noise wrapping-add; DropBatch в recover; heartbeat-фолбэк; delta=0 после
  сна; Close лога в Run; профиль бенчмарка + аллок-бюджеты; буферы-поля; сигнатура Fold
  ents []Entity; юнит-тест outbox; mark в заголовках + transport.Mark(); AckNotify-протокол;
  контрольные вне бюджета + граница ≥1; эпизоды лага в метрономе; backlog-вперёд; кольцевой
  старт; довод слайса указателей; benchstat-дисциплина; декларация outbox; дефолты;
  валидация конфига; критерий порядка фаз; seq ротации; переезды — фазой 4).
- S5 (2026-09-14): замыкание всеми 4 авторами — адресация подтверждена без переоткрытий
  (инвариантщик 6/6, конкурентщик 9/9, перформансер 8/8 с отзывом своей находки,
  прагматик 8/8). Пины замыкания внесены: семантика фолбэк-перечита (изъятие+применение
  одним шагом; frozen игнорирует фолбэк), H>0 в валидации, ссылка F49. Остаточный риск
  (шторм контрольных не ограничен ничем, кроме вотчдога — наблюдаемость, не защита) —
  принят осознанно. Выход в S6.
- S6 (2026-09-14): свежий взгляд, 3 сабагента без истории (архитектор, конкурентный
  практик, потребитель каркаса). Вердикты: «состоятельно» 3/3, рестарта нет. Принятые
  уточнения: F26 (delta в записи границы + период в заголовке — реплей обязан
  реконструировать LastDelta; двое независимо), F27 (политика ошибок записи +
  фрейминг), F28 (маркер паник-шага), F29 (StepResult: эффекты населения — fold
  вычисляет, актор применяет после дрена), F30 (EntityID рождений в логе), F31 (подписка
  шлюза на звонок метронома), F32–F38 (миноры + остаточный риск F38 — латентность
  кольца при устойчивом перегрузе, carry — фаза 4). Отклонено: период в State. Бюджеты:
  план 1/2 · рестарты 0/2.
- S8-раунд-3 (2026-09-14, СХОДИМОСТЬ): верификация микродофикса 1aa9ddc всеми
  авторами переоткрытий — F39, F67, F68 закрыты без остатков (инвариантщик 4/4,
  конкурентщик Н1–Н8+остатки, прагматик M1/m12/m14/ридер, тестировщик
  Б1/м13/м14/м7/м16; перформансер закрыл 5/5 ещё в раунде-2). Подтверждённых
  блокеров/мажоров — ноль. Выход из S8-цикла. Бюджеты: план 1/2 · рестарты 0/2 ·
  ревью реализации 3/3 (исчерпан — сходство достигнуто, не бюджетный выход).
- S8-раунд-2 (2026-09-14): верификация дофикса авторами. Переоткрытия: F39 — фикс
  применился наполовину (Stats не читал atomic; 5 ролей независимо, проба Store(99)→
  Backlog=1); новый мажор F67 — двойной дроп излишка K (проба: 24 при 20); миноры-остатки
  F68 (write-only метрика, молчаливые счётчики ридера, ротация-комментарий, регион 0,
  ассерты). Микродофикс раунда-3: Stats читает backlogLen + DrainOverrun; дизъюнктный
  покров (ctrlBatch+rereadBuf) + resetDrain на старте шага; счётчики ридера — ошибкой;
  ротация — файл из одного заголовка не ротачивается; first-флаг + fileSeq; ассерты
  серии/конфига/steps==0. Ворота зелёные; TestRegionPanicDropCoverDisjoint зелёный.
  Бюджеты: план 1/2 · рестарты 0/2 · ревью реализации 2/3.
- S8 (2026-09-14, раунд 1): панель 5 ролей слепо (инвариантщик, конкурентщик,
  перформансер, прагматик + тестировщик с двусторонней трассировкой). Трассировка:
  все пункты плана → артефакты; внеплановое — ExtractAppend (запись-обоснование выше),
  forcePanic-шов (аргументирован F23), depthTotal (перенесён в тесты в дофиксе),
  phaseLog как седьмая фаза маркера (соответствует по сути). Находки: F39 [блокер]
  (гонка Stats().Backlog — 4 роли), F40–F47 [мажоры] (потеря волны перечита в recover —
  воспроизведена; кэш проекции по длине — воспроизведён; паника ридера на злом uvarint;
  аллокация writeFrame на шаг — memprofile; флаки-race теста; кольцевой тест
  нефальсифицируем; бенч без контрольной смеси; нет теста ошибки записи), F48–F66
  (миноры: маркер тика, adviseBuf, перерасход-метрика, Birth.ID, header err, пустые
  файлы, ротация-негабарит, жизненный цикл сетов, watchdog-аллокации, тест-покрытия,
  baseline-текст). Все закрыты дофиксом (S8-дофикс): ворота зелёные, Idle 4→2 аллокации,
  baseline рев.2 со смесью. Бюджеты: план 1/2 · рестарты 0/2 · ревью реализации 1/3.
- S6-замыкание (2026-09-14): авторы F26–F38 подтвердили адресацию без переоткрытий.
  Пины реализации: guard на запись маркера F28 при уже отказавшем логе (один отказ, не
  цикл) — учитывается в S7. Обязательство вниз: P3.10 — развёртка NPC обязана идти через
  Births fold (логируется с EntityID; критерий F30 «население реплея равно прод»
  нарушится развёрткой вне шага) — S0-проверка задачи P3.10. План финален; вход в S7.
