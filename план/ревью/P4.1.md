# Реестр находок P4.1

- Задача: Сетка ячеек AoI — подъём вырожденной конфигурации фазы 3 (internal/replica,
  internal/world). Досрочный слот фазы 4 по распоряжению владельца (2026-09-18, после
  нагрузочного датчика: квадрат join-скана — граница ~5000 движущихся); полный план фазы 4
  (грид регионов, хэндоффы, бой) — отдельно, задача от него не зависит.
- Ревью-профиль: S2 — инвариантщик, перформансер, доменщик (3 роли по слову владельца);
  S8 — + тестировщик.
- Бюджеты: план 0/2 · рестарты 0/2 · ревью реализации 1/3

## Финальный план (S0, выносится на ревью)

Механика — буква ADR-0004 осей 2–3: сетка глобальна и выровнена глобально (не по границам
регионов), ячейка ≥ радиуса обзора, подписка наблюдателя — константный стенсил 3×3, блоб —
SoA-сегменты по ячейкам, слоты стабильные плотные с детектом реюза по вечному id. Сегодня
сетки нет: одна ячейка на регион (aoiCell=0), join-скан — каждый наблюдатель × все слоты =
O(N²); нагрузочный датчик 2026-09-18: 20000 игроков → 1.4 с/шаг при бюджете 100 мс
(benchmarks/P3.10-player-load-baseline.txt).

1. **replica: геометрия сетки (Grid-инжект).** `Grid{MinX, MinY, CellShift}` — значение
   с методом `CellOf(x, y int32) CellID` (арифметика в int64: вычитание min и сдвиг без
   int32-переполнения; компактный uint32 gx<<16|gy, диапазон домена 0..127 на ось;
   поведение вне домена — детерминированный кламп координат до вычитания, описан
   поведением, не аналогией с geoOf). Grid конструируется в world (`replica.NewGrid`) из
   экспортированных geo-констант домена (geo правится: экспорт WorldMinX/WorldMinY —
   единый источник, дублей констант нет) и передаётся в NewPublisher/NewJoin — реплика
   гео не импортирует (матрица ADR-0005 чиста). Норма размера (вердикт доменщика):
   **cellSize ≥ Exit** (не ≥ Enter: гистерезисное кольцо (3500,4200] обязано целиком
   лежать в окне 3×3; канон L2J 4096 < 4200 — осознанно не копируем, квирк); дефолт
   8192 = 2¹³ ≥ 4200. Фабрик-инвариант cellSize ≥ cfg.Exit (ошибка конструирования при
   нарушении — защита от тюнинга фазы 6 в осцилляцию).
   - Свойство-инвариант «окно 3×3 покрывает членство»: цель вне окна наблюдателя ⟹
     дистанция ≥ cellSize+1 > Exit 4200 > Enter 3500 (любая геометрия позиций в своих
     клетках, включая диагональные — вердикт доменщика). Следствия: ввод невозможен вне
     окна; член, покинувший окно, гарантированно за Exit → Remove легитимен; гистерезис
     enter/exit по дистанции работает только внутри окна. Property-тест на границах
     ячеек, включая позиции у ±(2³¹−1).
2. **replica.Blob: сегменты по ячейкам (плотная укладка).** `segs []segment` —
   сортированный слайс по CellID (пустых ячеек нет); segment = {cell; slotOff, slotLen
   (срез в общий слайс слотов блоба)}; общий `slots []int` (глобальные слоты по
   возрастанию внутри сегмента) + `records []Record` параллельно общему слайсу —
   3–4 аллокации на блоб-тик при любом числе ячеек (без пула — фаза 6). Слоты остаются
   **глобальным единым пространством издателя** (Publisher.slots как сегодня):
   view/Apply/детект реюза не меняются. Манифесты member/born/changed — глобальные
   битмапы по слотам (как сегодня); gone-битмап после изъятия gone-веток (п.4) не
   читается потребителем — ОСТАВЛЯЕТСЯ осознанно (диагностика целостности поколений и
   существующие тесты пакета; изъятие — отдельной гигиенической правкой, не этой
   задачей). `seatBySlot []seat` (слот → {cell CellID; ent
   EntityID} жильца поколения; для свободного слота cell = сентинел **cellInvalid** — не
   нулевая клетка, ноль валиден; ent = 0) — O(1) array-indexed доступ единой чистки
   (п.4в; детект деспавна и реюза — F2/F11). Порядок сегментов и слотов детерминирован
   (сортировка) — реплей/дампы стабильны.
3. **replica.Publisher.Build.** Распределение записей по ячейкам позиций; слот выдаётся как
   сегодня (младший свободный), запись попадает в сегмент своей ячейки; prev-сравнение
   полных пейлоадов — по-прежнему по слотам глобально (changed-бит от смены ячейки
   возникает сам: запись изменила Cell). Смена ячейки сущностью = перегруппировка списков
   сегментов, слот жив; маркеры переезда/hold-last НЕ порождаются (переезд между ячейками
   внутри региона — не хэндофф; два случая ухода ADR-0004 ось 4 не затронуты).
4. **replica.Join: окно вместо мира; единая чистка хвоста.** Шаг наблюдателя: (а) окно =
   3×3 ячейки вокруг ячейки наблюдателя (window(obsCell); разрешение ячеек — бинарный
   поиск по сортированному segs, ~9×log(#ячеек) на наблюдателя); (б) fullPass
   новорождённого/changed-наблюдателя и dirty-цикл — по слотам сегментов окна: вводы
   (Enter/born), удержание (beyondExit/!Visible), апдейты (changed — фикс F47);
   **gone-ветка dirty-цикла изымается** (её случаи забирает (в)); (в) — единственный
   источник Remove по **слотовым** признакам (вне окна / сентинел / чужой жилец);
   дистанционный выход за Exit и невидимость (!Visible) живого члена окна остаются
   в (б) — гистерезисный случай «обе клетки в окне» (F12);
   (в) **единая чистка per-члена — единственный источник EventRemove**: для каждого
   члена view — seat слота вне окна ИЛИ cellInvalid (деспавн) ИЛИ seat.ent ≠ id из view
   (реюз слота новым жильцом) → ровно один EventRemove по вечному id из view. Хвост
   fullPass (join.go:280 сегодня — сравнение жильца) и gone-ветка dirty — заменены
   чисткой (в), не дублируются: двойной Remove за шаг исключён структурно (F6/F11);
   чистка (в) исполняется на ВСЕХ путях шага — события и примирение reconcile — иначе
   view-хвост после паник-окна не чистится. Детект членства — событийный по dirty-потоку
   в пределах окна: стоимость на наблюдателя O(слотов 9 ячеек + |view|),
   детерминированно от толпы вне окна.
5. **replica.Snapshot/Read.** Advisory Read(cell, id) — по сегменту ячейки (линейный поиск
   внутри сегмента вместо всего населения). Сигнатуры не меняются.
6. **world.** NewRegion конструирует Grid (replica.NewGrid(geo.WorldMinX, geo.WorldMinY,
   дефолт cellShift 13 — единая точка константы для фабрик-инварианта F3) и передаёт в
   NewPublisher/NewJoin; Grid хранится регионом для recordOf: Cell = grid.CellOf(x, y)
   (const aoiCell умирает); aoiStep/composeJoin/JoinConfig — без изменений.
7. **Числа.** cellSize 8192 ≥ Exit 4200 (канон PlayerKnownList: watch 3500/forget 4200 —
   OQ-3 закрыт P3.8); точный размер — фаза 6 по метрикам (ADR-0004 ось 2), 8192 —
   инженерный дефолт (степень двойки, 2× радиуса — окно 3×3 покрывает гистерезис с
   запасом).
8. **Датчики/бенчи.**
   - Нагрузочная лестница BenchmarkRegionPlayerLoad (до/после — та же методика):
     критерий-цель: 10000 кластеры50 ≤ 60 мс/шаг (сейчас 361 мс); 20000 — зафиксировать
     факт. Разложение прогноза (пересчёт ревью-перформансера по до-числам: кадр ≈ 60 нс,
     слот-итерация ≈ 3.4 нс, Build ≈ 0.56 мс/10k): кадры 29/59 мс (доминанта; слэб-пул —
     фаза 6) + скан окна fullPass при all-dirty 10–26 мс + чистка хвоста 1.5/3 мс +
     Build с сегментами 0.65/1.3 мс ⇒ 10000 ≈ 42 мс (38–50) — критерий с запасом; 20000
     ≈ 85 мс (78–95) — на грани бюджета, резерв 5–20% (записать как есть; толпа-1000 в
     одной ячейке не улучшается и не должна — фундаментальный N² кадров, стена
     ~1000–1700 в точке по ADR-0004 «Проверка»).
   - Фальсификатор «независимость от толпы вне окна» (тест-обязательство ADR-0004 фазы 4):
     население вне окна наблюдателя растёт на порядки — стоимость **изолированного
     Join.Step на наблюдателя** стабильна с точностью до log(#ячеек) (бинарный поиск
     segs); полный шаг не годится датчиком (Build линеен по всему населению).
   - До-базлайн join/PublishBlob перегнать на этой машине (P3.8-файл снят на windows —
     чужая машина, perf.md §0); сверка до/после на одном железе. Ожидание по Build
     (плотная укладка п.2): +3–4 аллокации на блоб-тик при любом числе ячеек; бюджет
     Idle-шага 15 → правка осознанная, с новой раскладкой в комментарии (OQ-3 закрыт
     числами).
9. **Тесты.**
   - Юнит-свойства: CellOf (границы клеток, края мира, детерминизм); сегментация блоба
     (записи по ячейкам, слоты глобальные, порядок сортирован); смена ячейки целью
     (перегруппировка, changed-бит, без маркеров).
   - Join-свойства: пара через границу ячеек (ввод по Enter, апдейт стрима, выход за Exit);
     выход цели из окна → ровно один Remove; реюз слота нового жильца: вне окна И в окне
     при covered-наблюдателе (двинувшийся наблюдатель + деспавн члена + рождение на его
     слоте — ровно один Remove старого по вечному id + Introduce нового, F11); возврат
     в окно → повторный ввод (после Enter); деспавн члена внутри окна → ровно один
     Remove (cellInvalid, F2); после паник-окна — примирение доезжает чистку (в); наблюдатель пересекает границу ячейки — окно едет, известность
     не мерцает (член новой кромки вводится, ушедшей — Remove; смена окна — дальняя
     кромка ≥8193, члены ≤4200 не задеты); гистерезис-дуга на краю Enter — нулевой чурн
     (перенос P3.8 на сетку); covered-наблюдатель в окне (твин F47).
   - Property set-equality на сетке (тест-обязательство ADR-0004, обобщение вырожденного
     «по населению региона»): случайные расстановки/перемещения/деспавны в нескольких
     ячейках — view каждого наблюдателя == эталонная функция членства (дистанции
     Enter/Exit, гистерезис) после каждого шага; ловит рассинхрон чистки и вводов.
   - Вырожденная конфигурация: все записи в одной ячейке — все существующие тесты join/
     region без правок (закон оси 6); прогон полного пакета — регрессий нет.
   - e2e: движущаяся пара через границу ячеек — CharMoveToLocation продолжает доставляться
     (живой контур, синтетика: расставить спавны/маршрут через границу 8192).
10. **Скоуп-границы (не входит).** LOD-тиры, per-client outbound budget, дельта-пейлоады,
    пулы/encode-once (фаза 6); хэндоффы регионов, затравки, маркеры переезда, слуги,
    предметы лёгкого класса, инстанс-пространства (прочая фаза 4); изменение JoinConfig
    (радиусы канона не трогаем).

Открытые вопросы — закрыты ревью раунда 1 (вердикты в «Раундах»):

- OQ-1 закрыт: норма cellSize ≥ **Exit** (гистерезисное кольцо целиком в окне 3×3; канон
  L2J 4096 < 4200 — квирк, не копируем); 8192 = 2¹³; фабрик-инвариант связки (F3).
- OQ-2 закрыт: gx<<16|gy (диапазон домена 0..127 на ось — запас до cellSize 2048 фазы 6);
  вычисление в int64, кламп вне домена (F9/F10).
- OQ-3 закрыт числами: плотная укладка сегментов — 3–4 аллокации на блоб-тик при любом
  числе ячеек (F7); бюджет Idle-шага — осознанная правка с раскладкой.
- OQ-4 закрыт: чистка O(|view|) ≤ O(слотов 9 ячеек) по свойству п.1 (каждый член — в
  окне); в толпе ~1–2 мс на 1000 — в разбросе (вердикт перфомансера).

## Находки

| ID | Стадия | Роль | Утверждение | Свидетельство | Серьёзность | Статус | Решение |
|---|---|---|---|---|---|---|---|
| F1 | S2 | инвариантщик | границы мира в replica: либо импорт geo (ребро против матрицы ADR-0005 — replica→geo запрещён), либо дубль констант домена (второй источник истины) | checkdeps.sh:21 (replica — только transport); geo-константы домена не экспортированы | мажор | подтверждена | Grid-инжект: geo экспортирует WorldMinX/Y (единый источник), world конструирует replica.NewGrid и передаёт в NewPublisher/NewJoin — ноль новых рёбер |
| F2 | S2 | инвариантщик | деспавн в сегментном лейауте не детектится: сегменты несут только занятые слоты — gone-бит свободённого слота никем не итерируется; cellBySlot свободного слота неспецифицирован → вечный фантом (сценарий: цель-член деспавнится, наблюдатель неподвижен) | механика сегментов п.2 плана vs join.go:203–214 (сегодня dirty идёт по records с дырками); нулевая клетка CellID(0) валидна | мажор | подтверждена | cellBySlot со сентинелом cellInvalid; единая чистка хвоста (п.4в): вне окна ИЛИ сентинел → ровно один Remove |
| F3 | S2 | инвариантщик | нет связки-инварианта cellSize ≥ JoinConfig.Exit: фаза 6 может поставить cellSize < Exit → Remove внутри гистерезисного кольца → осцилляция | радиусы и cellSize в разных местах; ADR ось 2 оставляет размер фазе 6 | минор | подтверждена | фабрик-инвариант в Grid/Join-конструировании (ошибка при cellSize < Exit) |
| F4 | S2 | инвариантщик | обобщение set-equality на сетку не заявлено — попарные тесты не ловят рассинхрон чистки и вводов | ADR-0004 «Последствия» фазы 3: «join = set-equality» | минор | подтверждена | property set-equality на сетке против эталонной функции членства (п.9) |
| F5 | S2 | перформансер | прогноз не разложен по членам: скан-окна fullPass при all-dirty 10–26 мс не назван; резерв 20000 до бюджета — 5–20%, не 40% | пересчёт: кадр ≈60 нс, слот ≈3.4 нс; join.go:187 (changed → fullPass) | минор | подтверждена | разложение прогноза в п.8 с числами (10000 ≈ 42 мс, 20000 ≈ 85 мс) |
| F6 | S2 | доменщик | тройная чистка ((в)+(г)+хвост fullPass) без правила исключительности — двойной EventRemove одного члена за шаг (реюз слота вне окна) | сценарий: деспавн X слот 5, новый жилец вне окна; join.go:280 хвост | минор | подтверждена | единая чистка per-члена (п.4в), хвост fullPass заменён ею; тест-кейс реюза вне окна (п.9) |
| F7 | S2 | перформансер | слайсы per-сегмент → ~2×#ячеек аллокаций Build (800 на 20k) — без чисел в OQ-3 | оценка по лейауту п.2 | минор | подтверждена | плотная укладка: общий слайс слотов + оффсеты сегментов — 3–4 аллокации при любом числе ячеек (п.2) |
| F8 | S2 | перформансер | фальсификатор «стоимость стабильна» — (а) log-рост бинарного поиска, (б) полный шаг не годится (Build линеен по населению — забьёт сигнал) | п.8 формулировка | минор | подтверждена | датчик = изолированный Join.Step на наблюдателя; допуск log(#ячеек) (п.8) |
| F9 | S2 | перформансер | до-базлайн join/PublishBlob снят на windows — сверка до/после обязана быть на одной машине (perf.md §0) | заголовок P3.8-replica-baseline.txt | минор | подтверждена | перегнать до-базлайн на целевой машине перед реализацией (п.8) |
| F10 | S2 | доменщик | CellOf: x−minX в int32 переполняется при x→2³¹−1 → мусорный CellID; «как geoOf» двусмысленно (geoOf паникует) | minX=−655360; прецедент P3.8 F20/F53 | минор | подтверждена | арифметика int64 + кламп вне домена, поведением (п.1); края ±(2³¹−1) в тест-генераторе |
| F12 | S5 | замыкание | противоречие формулировок: «(в) — единственный источник EventRemove» против сохранённого beyondExit/!Visible-Remove в (б) — буквальное чтение вешает члена за Exit внутри окна | п.4 строки 59–63; join.go:223/264 эмитят Remove в удержании | минор | подтверждена | (в) — единственный источник Remove по слотовым признакам; дистанционный выход/невидимость — в (б) (п.4); тест «выход за Exit» ловил бы буквализм |
| F11 | S5 | замыкание (класс F2/F6) | реюз слота В окне при covered-наблюдателе: Remove старого жильца не покрыт — fullPass не имеет gone-ветки, хвост fullPass (ловящий реюз сравнением id жильца, join.go:280) планом заменён на чистку (в), которая для занятого слота в окне молчала → вечный фантом | join.go:187–190 covered → fullPass; :199 скип dirty; сценарий: двинувшийся O + деспавн X + рождённый N на слоте X | мажор | подтверждена | чистка (в) расширена: seat.ent ≠ id из view → Remove (сравнение жильца — из сегодняшнего хвоста fullPass); gone-ветка dirty изымается — Remove единственным путём (п.4); кейс «реюз в окне + covered» (п.9); чистка на всех путях включая reconcile |
| F13 | S6b | тест-планировщик | оракул property set-equality обязан быть симуляцией с состоянием (гистерезис по истории вводов), не чистой функцией позиций — иначе S7 напишет нефальсифицируемый эталон | план п.9 «эталонная функция членства (дистанции Enter/Exit, гистерезис)»: членство при Enter<d≤Exit зависит от истории, функции позиций недостаточно | минор | подтверждена | тест-план: эталон — известностная модель с состоянием (гистерезис по истории) |
| F14 | S6b | тест-планировщик | фальсификатор стоимости (п.8) — не красный/зелёный CI-тест: сверка benchstat до/после на одной машине; функциональный пин (события не зависят от толпы) на квадрат-скане старого кода зелёный | план п.8: датчик = изолированный Join.Step, допуск log(#ячеек) — тайминговый ассерт в CI флаки (testing.md §1) | минор | подтверждена | тест-план разделяет: функциональный пин — тест; стоимость — бенч-датчик с benchstat-сверкой (F8/F9-методика) |
| F15 | S6b | тест-планировщик | e2e-синтетика не зафиксирована: ближайшая граница сетки от дефолтных позиций харнесса (x=−71338, e2e_test.go:468) — −73728 (2390 юнитов); маршрут ~2.7k юнитов превышает 3-с бюджеты waitFor | арифметика: (−71338+655360)/8192=71.29 → граница k=71 = −73728 | минор | подтверждена | тест-план: позиции синтетикой у границы (прецедент TestE2EMovementObserverOutsideRadius), бюджет прибытия ≥30 с, промежуточные ассерты по первому кадру стрима |
| F16 | S6b | тест-планировщик | «все существующие тесты join/region без правок» (план п.9) невыполним буквально: белые тесты читают b.seg.records и зовут NewJoin/NewPublisher — сигнатуры и лейаут меняются | join_test.go:42–43, :229, :254 — прямые чтения seg.records; план п.1 меняет сигнатуры конструкторов | минор | подтверждена | уточнение нормы: правки сигнатур/белых чтений лейаута допустимы, ассерты поведения — нет; вырожденная конфигурация = прогон пакета зелёным |
| F17 | S7 | реализация (пойман TestJoinPropertyGridSetEquality) | столкновение слотов: свежий слот = maxRetained+1 игнорировал свободные слоты выше удержанных (хвостовые жильцы прошлых поколений ушли, экстент усох) — двум новорожденным выдавался один слот, resolveObs скипал второго наблюдателя пары | сид 8/шаг 9: slotOf[23]=slotOf[24]=16, событие ввода только в одну сторону; стартовая длина сегодняшнего Build включала len(prev.records) — монотонность экстента потеряна при переходе на плотную укладку | мажор | подтверждена | экстент слотов монотонен: fresh/seats/битмапы тянут len(prev.slotPos); реюз остаётся младшим свободным |
| F18 | S7 | реализация (пойман профилем датчика) | чистка хвоста итерирует всё слот-пространство view (10k слотов ради ~49 членов) — O(слоты), не O(|view|) плана п.4; лестница 10000 = 75.7 мс при критерии ≤60 | pprof датчика: cleanupTail 26% CPU (0.93s flat), ещё 9% — линейный поиск окна в seatInWindow | мажор | подтверждена | битмап членства в viewSet (обход по словам, нулевые скипаются) + арифметика смежности клеток O(1); после: 10000 = 48 мс — критерий выполнен |
| F19 | S8 раунд 1 | перформансер | Event несёт Record по значению (~200 Б × 2–3 копии на событие): 490k событий/шаг на лестнице 10k — ~23% шага; perf.md §1 «передавать указатель»; не измерено и не записано | изолированный эксперимент ревьюера: Target *Record даёт Build+Step+Apply 25.3→14.3 мс (−42%), аллокации идентичны; 20000 aoi-медиана 100.4 мс > бюджета 100 | мажор | закрыта (правкой) | Event{Target *Record — запись блоба шага (иммутабельна в окне Step→compose→Apply), Entity — вечный id (при Remove Target=nil)}; замыкание числами: лестница 10000 50.9→34.9/35.6 мс, 20000 99.8/100.4→76.8/76.1 мс (бюджет 100 с запасом), 1000 толпа 60–67→47 мс |
| F20 | S8 раунд 1 | тест-практик | флак пакета replica: в начале сессии ревьюера 5/11 прогонов -count=2 красные, симптом «Remove не эмитится при деспавне/реюзе» (класс F2/F11); -race молчит; далее 150+ зелёных, невоспроизведён | логи прогона практики (join_grid_test.go:180,207,229,254,283,404; join_test.go:309,355,567); единственный недетерминизм пакета — порядок map-итерации сбора departed | мажор | закрыта (правкой) | корень-кандидат F21 устранён (slices.Sort(free) — единственный недетерминизм map-порядка пакета); верификация: 8×-count=2 зелёные + -race + property 100 итераций; воспроизведение инцидента отсутствует — зафиксировано честно |
| F21 | S8 раунд 1 | инвариантщик | «младший свободный слот» не гарантировался: free = p.free + departed конкатенировался без общей сортировки — выбор слота новорожденным зависел от рандомизированного map-порядка | трасса: пок.1 A→0,B→1; пок.2 B деспавн free=[1]; пок.4 A деспавн+C рождение → C на слоте 1, слот 0 свободен | минор | подтверждена | slices.Sort(free) после конкатенации + TestPublisherFreeListLowestReuse (чередующиеся деспавны, сортированность, младший реюз) |
| F22 | S8 раунд 1 | тестировщик + инвариантщик | property set-equality 40 итераций вместо 100 плана, без краёв домена в генераторе | план S6b «100 итераций … края домена» vs iter<40, randCellPos без краёв | минор | подтверждена | 100 итераций + края домена/int32 в генераторе |
| F23 | S8 раунд 1 | тестировщик | компонентный стрим: маршрут 40 юнитов ≈ 3 тика, moves≥2 пропускал потерю кадра; комментарий арифметически неверен | регион 10 Гц → 11.5 юн/тик; проба ревьюера moves=3 | минор | подтверждена | маршрут 200 юнитов (~17 тиков), moves ≥ 12, комментарий с арифметикой |
| F24 | S8 раунд 1 | тестировщик + тест-практик | мёртвый код: maxSlot пишется и не читается; `_ = fmt.Sprint()` держит импорт; текст «100 клетках» при фактических ~1000 | blob.go/join_grid_test.go/blob_grid_test.go | минор | подтверждена | удалено |
| F25 | S8 раунд 1 | доменщик | домен оси 0..0x7FFF против «0..127» плана — буквальная арифметика плана неисполнима (при cellSize 2048 мир требует 512 клеток на ось) | grid.go maxCellAxis = 1<<15−1; сентинел cellInvalid = 1<<31 требует старший бит | минор | подтверждена | отклонение записано (эта строка): 15 бит на ось, старший бит — сентинел; «0..127» — фактическое число клеток при 8192, не граница домена |
| F26 | S8 раунд 1 | доменщик + тест-практик | ослабленные оракулы: enter-дуга инварианта окна не фальсифицировалась (мёртвый ассерт, `_ = enter`); домен-тест региона — тавтология | grid_test.go мёртвое `minD2 > 4*cell*cell`; grid_domain_test.go — тот же вызов CellOf с обеих сторон | минор | подтверждена | явная enter-проверка (пара ≤ Enter² ⟹ клетки соседние) + декод-проверка [0,0x7FFF]²/сентинел |
| F27 | S8 раунд 1 | перформансер + тест-практик | фальсификатор/MultiCell: метки клеток не соответствовали раскладке (cells=10000 → ~10⁵ клеток); измерялся примирительный путь (Apply в цикле) | формула (i%cells, (i/cells)%cells) даёт cells² точек | минор | подтверждена | честная раскладка (кластер = клетка, ровно cells), Apply убран из цикла — стационарный dirty-путь |
| F28 | S8 раунд 1 | перформансер | артефакт replica-after смешивал два бинаря (первый блок — промежуточный харнесс с Build в метрике: 15–27 мс/op, 29.6 МБ/op), цитаты фальсификатора «119/141/150 нс» отсутствовали в артефакте | строки 83–112 файла против 48 B/op на HEAD | минор | подтверждена | единый перегон на финальном HEAD, числа в шапках — из артефакта |
| F29 | S8 раунд 1 | перформансер | атрибуции регрессий: JoinSteadyDirty +53.6 мкс объяснялся «+2 аллокации» (0.1 мкс) — реальный драйвер join-часть 44→100 мкс (чистка/refs/поиск сегментов — вилка OQ-4); Build N10000 +24% против прогноза 1.3 мс без записи | вычитание Build из PublishBlob до/после | минор | подтверждена | атрибуции исправлены; отклонение Build от прогноза — этой записью |
| F30 | S8 раунд 1 | перформансер | 20000: aoi-медиана 100.4 мс > бюджета 100 — запись цитировала только step-медиану 99.8 | P4.1-player-load-after.txt (старый перегон) | минор | подтверждена | обе медианы явно в шапке (финальный перегон — см. Раунды) |
| F31 | S8 раунд 1 | перформансер | sort.Interface на горячем пути Build: interface-конверсия +1 аллокация/тик, виртуальные вызовы | замена ревьюера: PublishBlob N10000 1.70→1.64 мс, 76→75 аллокаций | минор | подтверждена | slices.SortFunc(byCellSlot); бюджет Idle 17→16, раскладка обновлена |
| F32 | S8 раунд 1 | перформансер | абсолют аллокаций в комментарии теста занижен (~8–9 против измеренных 19–20 на 1000 записей) | прогон TestBuildAllocsIndependentOfCellCount | минор | подтверждена | комментарий: 19 на 1000 записей / 8 на ~100 (Idle) — зависит от населения, не от клеток |
| F33 | S8 раунд 1 | тест-практик | ВНЕ ДИФА P4.1: тайминговая фальсификация FAF-дропа (world/stress_test.go) флакает на быстром железе без -race — исключение только raceEnabled | 1/6 прогонов ревьюера красный: Hz=1000 успевает дренить волну 2048 до переполнения | минор | подтверждена | вне скоупа слайса: кандидат в ближайший слайс фазы 4 (синхронная фальсификация или build-тег) |

## Отклонённые (память анти-петли)

| ID | Утверждение | Причина отклонения | Раунд |
|---|---|---|---|

## Исправления

| ID | Правка | Аргументация (альтернативы и почему хуже) | Чем проверяется закрытие |
|---|---|---|---|
| F1 | Grid-инжект из экспортированных geo-констант | альтернативы: импорт geo в replica (ломает матрицу — ворота красные) и дубль констант (дрейф источников при эволюции гео) — обе хуже; экспорт двух констант из geo — нулевая цена | финальный план п.1; checkdeps зелёный |
| F2/F6 | cellBySlot-сентинел + единая чистка per-члена | альтернатива — entBySlot-массив жильцов: та же цена, второй индекс без нужды; единая чистка убирает двойной Remove классом, а не тестом | финальный план п.2/п.4; тесты деспавна-в-окне и реюза-вне-окна (п.9) |
| F3 | фабрик-инвариант cellSize ≥ Exit | тест-проверка слабее: не защищает фазу 6 от тихой осцилляции | финальный план п.1 |
| F4 | property set-equality на сетке | попарные кейсы — по одному сценарию; property покрывает класс | финальный план п.9 |
| F5/F7/F8/F9 | разложение прогноза; плотная укладка; Step-датчик с log-допуском; до-базлайн на этой машине | уточнения методики; критерии приёмки не менялись | финальный план п.8 |
| F10 | int64-арифметика CellOf + кламп | проверка после факта (тест) не чинит мусорный CellID в рантайме | финальный план п.1; края в тест-генераторе |

## Тест-план (S6b)

Уровни по docs/testing.md §3. Белый ящик — пакетные тесты `package replica`; компонентные —
регион-харнесс (метроном/транспорт/лог/pushCollector реальны, шаги из тест-горутины =
single-writer); e2e — живой контур cmd/l2go с l2client. Приоритеты: ПБ — приёмка-блокер, В —
высокий, Ж — желательный. Статусы кейсов ведёт S7: планировался → реализован / отклонён (с
аргументом).

### Юнит — internal/replica (белый ящик)

| Кейс | Трассировка | Сценарий → Оракул (что фальсифицирует) | Пр. | Статус |
|---|---|---|---|---|
| TestGridCellOfBoundariesAndClamp (таблица) | п.1; F10 | x=MinX, MinX±1, MinX+k·8192±1, край мира ±1, 0, ±(2³¹−1) → CellOf == эталонной int64-арифметике; кламп вне домена к краевой клетке идемпотентен; decode gx=c>>16, gy=c&0xFFFF ∈ [0,127]; без паники. Ловит int32-переполнение (F10) и знаковый сдвиг | ПБ | реализован |
| TestGridCellOfPropertyDomainBoundedDeterministic | п.1 | 1000 случайных int32-пар → CellOf дважды: чистая функция, decode всегда 0..127 | В | реализован |
| TestGridWindowCoversMembershipProperty | п.1 (инвариант) | офсеты клеток −2..2 × позиции в углах: клетки не обе в окне 3×3 ⟹ min-дистанция ≥ cellSize+1 > Exit; обратно d≤Enter ⟹ клетка цели в окне, включая диагонали. Ловит разрыв связки cellSize≥Exit на геометрии | ПБ | реализован |
| TestNewGridRejectsCellSizeBelowExit | п.1; F3 | NewGrid+NewJoin: cellSize < Exit → error; == Exit → допустимо. Ловит тихую осцилляцию фазы 6 | ПБ | реализован |
| TestBlobSegmentsPerCellLayout | п.2 | население в 3+ клетках → segs сортированы по CellID, пустых нет; слоты глобальные, возрастают внутри сегмента; seatBySlot: занятый {cell, ent}, свободный → cellInvalid (≠ нулевая клетка, F2) и ent=0; два Build идентичны (реплей/дампы стабильны) | ПБ | реализован |
| TestBlobSingleCellDegenerateLayout | п.9, корнер 0/1 | 0 записей → 0 сегментов, Step/Apply без паники; 1 запись → 1 сегмент; все в одной клетке → 1 сегмент, слоты плотные | В | реализован |
| TestPublisherCellChangeRegroupsSlotLives | п.3 | цель A→B (обе в окне) → слот жив, changed взведён, gone/born нет, Moving пуст; наблюдателю ровно один EventUpdate, ноль Remove/Introduce. Ловит ложный Remove за смену клетки | ПБ | реализован |
| TestJoinPairAcrossCellBoundaryLifecycle | п.4(б); F12 | (1) соседние клетки d≤3500 → Introduce; (2) движение вдоль границы 5 шагов → только Update; (3) d>4200, обе клетки в окне → ровно один Remove (ловит буквализм F12); (4) цель из окна → ровно один Remove | ПБ | реализован |
| TestJoinHysteresisArcAcrossCellBoundaryZeroChurn | п.9 | дуга в кольце (3500,4200] с пересечением границы клетки: churn=0 (в т.ч. при смене клетки — только Update); 1 Remove / 1 Introduce на переходах; возврат в кольцо — 0 событий | ПБ | реализован |
| TestJoinObserverWindowShiftNoFlicker | п.9 | наблюдатель через границу (changed → fullPass): члены ≤4200 — ноль Remove (дальняя кромка ≥8193 их не задевает); ушедший за Exit — Remove; новая кромка — Introduce | ПБ | реализован |
| TestJoinTargetReturnsToWindowReintroduces | п.9 | уход в клетку вне 3×3 → Remove; возврат ≤3500 → Introduce; снова вне → Remove | В | реализован |
| TestJoinDespawnInWindowSingleRemove | п.4(в); F2 | деспавн члена окна при неподвижном наблюдателе → seat.cell=cellInvalid → ровно один Remove по вечному id; gone-ветки dirty больше нет | ПБ | реализован |
| TestJoinSlotReuseOutsideWindowSingleRemove | п.4(в); F6 | деспавн X, новый жилец Y на слоте X вне окна → ровно один Remove(X), ноль Introduce(Y) | ПБ | реализован |
| TestJoinSlotReuseInWindowCoveredObserver | п.4(в); F11 | двинувшийся наблюдатель + деспавн X + рождение N на слоте X в окне → ровно один Remove(X) + Introduce(N); уход N за Exit → Remove(N) | ПБ | реализован |
| TestJoinPanicWindowReconcileRunsTailCleanup | п.4 (все пути) | ForcePanicInApply на шаге с деспавном → recover → следующий шаг: примирение доезжает чистку (в), view чист, далее 0 событий | ПБ | реализован |
| TestJoinPropertyGridSetEquality | п.9; F4 | 100 итераций случайных расстановок/перемещений/деспавнов в 2–4 клетках (генератор таргетит границы клеток, кольцо, края домена, хвостовые дырки) → view == эталонная симуляция членства с состоянием (F13) | ПБ | реализован |
| TestJoinStepStagesIdempotentOnGrid | твин P3.8 | много-клеточный блоб → Step дважды без Apply: события идентичны, view нетронут | В | реализован |
| TestJoinBothMovingAcrossCellBoundaryUpdate | твин P3.10-F47 | наблюдатель и цель оба changed, пересекают границу → ровно один EventUpdate пары | В | реализован |
| TestJoinWindowImmuneToOutsideCrowd | п.8 (пин) | изолированный наблюдатель + член; толпа вне окна 10³→10⁵ в 1 и 10³ клетках → события бит-в-бит не зависят от толпы (стоимость — бенчем, F14) | В | реализован |
| TestPublisherReadRoutesByCellSegment | п.5 | запись в клетке B: Read(B,id) hit, Read(A,id) miss; после переезда B→C: Read(B) miss, Read(C) hit | В | реализован |
| TestBuildAllocsIndependentOfCellCount | п.2; F7/OQ-3 | AllocsPerRun(Build+Commit): 1000 записей в 1 клетке vs 100 клетках — счёт равен ±0; абсолют числом в комментарии | ПБ | реализован |
| TestReplicaExportedAPISurface (правка эталона) | приватность | go/ast-эталон: +Grid, +NewGrid; cellInvalid НЕ экспортируется | ПБ | реализован |

### Юнит — geo, world

| Кейс | Трассировка | Сценарий → Оракул | Пр. | Статус |
|---|---|---|---|---|
| TestGeoWorldConstantsExported | п.1; F1 | WorldMinX/Y == −655360/−589824 — пин единого источника | Ж | реализован |
| TestRegionGridFromGeoCoversWorld | п.6; F1 | NewRegion; спавны на краях домена и за ними → recordOf(ent).Cell == grid.CellOf(pos), клетки в домене; aoiCell изъята | В | реализован |

### Компонентные — internal/world

| Кейс | Трассировка | Сценарий → Оракул | Пр. | Статус |
|---|---|---|---|---|
| TestRegionKnownSetAcrossCellBoundary | п.6 | пара по разные стороны границы 8192 (ближайшая к дефолту: −73728), d<Enter → взаимный ввод; расходение за 8192 → разрыв | ПБ | реализован |
| TestRegionMovementStreamAcrossCellBoundary | п.9 | маршрут пары через −73728, ~25 шагов → CharMoveToLocation каждый шаг, 0 DeleteObject/CharInfo после первичного ввода, StopMove на прибытии | ПБ | реализован |
| TestRegionPanicInApplyDespawnReconcileCleanup | п.4 | живой Run, ForcePanicInApply на шаге ухода члена → снятие → шаги: knownOf сходится, фантома нет (живость+чистота, не after<before) | ПБ | реализован |
| TestRegionReenterNearCellBoundarySameKnownSet | п.9 | перезаход у границы → известность по id идентична (прецедент TestE2EReenterNpcSetIdenticalById) | В | реализован |
| Существующий пакет целиком | п.9 (F16) | прогон join/region/e2e зелёный; правки сигнатур/белых чтений допустимы, ассерты поведения — нет | ПБ | реализован |

### E2E — cmd/l2go

| Кейс | Трассировка | Сценарий → Оракул | Пр. | Статус |
|---|---|---|---|---|
| TestE2EPairMoveAcrossCellBoundary | критерий «e2e» | Alice/Bob у границы (синтетика позиций + restartGS, F15), MoveToLocation через −73728 (~2.7k, бюджет ≥30 с) → трафик-лог Bob: первичный CHAR_INFO; непрерывный CHAR_MOVE_TO_LOCATION через границу; ноль спонтанных DELETE_OBJECT/CHAR_INFO ≥3 тиков (молчун-инвариант); STOP_MOVE на прибытии | ПБ | реализован |

### Датчики (бенчи — журнал, не CI-ворота)

| Кейс | Трассировка | Сценарий → Оракул | Пр. | Статус |
|---|---|---|---|---|
| BenchmarkRegionPlayerLoad (та же методика) | критерий ≤60 мс | лестница 100/1000/10000/20000 × crowd/clusters50 × step/aoi без правок; живость frames/op и aoiObs | ПБ | реализован |
| BenchmarkJoinStepIsolatedFromCrowd | п.8; F8 | 1 наблюдатель + 1 член; толпа вне окна 10⁵, клетки 1/10²/10⁴ (Grid-инжект с мелким доменом); Build вне метрики; ns/op стабилен с допуском log(#ячеек) | ПБ | реализован |
| TestRegionStepIdleAllocBudget (правка) | п.8; OQ-3 | новое число бюджета Idle с раскладкой в комментарии (~16–17, блоб-тик ~8–9 аллокаций) | ПБ | реализован |
| BenchmarkPublishBlobMultiCell, BenchmarkAdvisoryReadMultiCell | п.2/п.5 | лестницы N × {1, 100 клеток}: PublishBlob без наклона по клеткам; Read линеен по сегменту | В/Ж | реализован |

### Трассировка критериев приёмки → кейсы

1. 10000 кластеры50 ≤ 60 мс — BenchmarkRegionPlayerLoad + до-базлайн на этой машине
   (benchmarks/P4.1-replica-before-baseline.txt, P3.10-player-load-baseline.txt) + ворота.
2. Фальсификатор независимости — BenchmarkJoinStepIsolatedFromCrowd (стоимость) +
   TestJoinWindowImmuneToOutsideCrowd (функциональный пин).
3. Property-тесты — TestGridCellOfBoundariesAndClamp, TestGridWindowCoversMembershipProperty,
   TestJoinHysteresisArcAcrossCellBoundaryZeroChurn, TestJoinPropertyGridSetEquality,
   TestBlobSingleCellDegenerateLayout + существующий пакет (вырожденная).
4. e2e через границу — TestE2EPairMoveAcrossCellBoundary (+ компонентная репетиция).
5. Ворота — build/vet/staticcheck/test -race/checkdeps (F1 — checkdeps).
Непокрываемых критериев нет.

### Корнер-чеклист (пройден S6b)

Границы 0/1/макс/±1/переполнение — есть (пустые segs; ±1 у границ клетки и домена; края
int32; клетка 0 валидна против cellInvalid; окно у края домена без заворота). Интерливинги —
паник-окна по всем швам ForcePanicInApply/forcePanic(phase)/forcePanicPostApply; дубль-Remove
исключён структурно, фальсифицируется кейсами 12–14; параллельные завершения/поздние ответы —
не применимо (replica синхронна, без горутин и дедлайнов). Тайминги — гистерезис-дуга (перезавод
домена); пропуск тика — механика метронома, не скоуп; e2e-молчание ≥3 тиков — инвариант, не
синхронизация. Деградации — Grid с cellSize<Exit (фабрик-инвариант), координаты вне домена
(кламп); I/O-деградации не применимы (библиотека). Злые входы — пустое и края int32 есть;
сеть/парсинг не в скоупе. Чурн/рестарты — перезаход у границы; персистентности у replica нет.
Пики — толпа в одной ячейке осознанно не улучшается (N² кадров, ADR-0004 «Проверка»);
crowd-подкейсы ≤1000 с ООМ-гвардом; вне-окно толпа не страдает — фальсификатор. Идемпотентность
— повторный Step без Apply; стационар после примирения = 0 событий.

### Анти-план

Интерналы сортировки segs; gone-битмап (потребителя нет — осознанное оставление); валидация
JoinConfig и адаптивные тиры (п.10); бенч-пороги как CI-ворота; моки Grid/Advisory (инжект
реальный); тривиальные аксессоры; слэб-пул/encode-once/delta (фаза 6); crowd>1000; хэндоффы/
маркеры (не порождаются — закрыто существующим TestJoinGoneWithoutMarkerRemovesImmediately);
байтовое представление CellID вне декод/домен-свойств.

### Требуемые швы (обязательство S7)

1. Конструкторский: NewPublisher(grid) / NewJoin(grid, cfg); Grid = replica.NewGrid(minX,
   minY, shift) с произвольным доменом (плотная раскладка фальсификатора).
2. Фабрик-инвариант cellSize ≥ Exit — наблюдаемая ошибка, не молчание.
3. JoinConfig-инъекция — есть, без изменений.
4. Паник-швы ForcePanicInApply/forcePanic(phase)/forcePanicPostApply — есть, достаточны.
5. Часы/тикер/канал-оракул/остановка актора — не нужны (нет дедлайнов, Step синхронен,
   replica без горутин). Белый доступ к segs/seatBySlot/window — пакетные тесты, не экспорт.

## Раунды

- Раунд 1 (S2→S4, 2026-09-18): 3 роли по слову владельца — инвариантщик (2 мажор +
  2 минор), перформансер (4 минор + пересчёт прогноза: 10000 ≈ 42 мс — критерий ≤60
  проходит; 20000 ≈ 85 мс (78–95) — план был консервативен в безопасную сторону),
  доменщик (2 минор + центральный вердикт: смена ячейки ≠ исчезновение из заголовка
  членства — слот жив, «два случая ухода» оси 4 не задеты; канон L2J 4096 < Exit —
  квирк, норма cellSize ≥ Exit). Все свидетельства верифицированы по коду; OQ-1..OQ-4
  закрыты; план переработан (Grid-инжект, единая чистка, плотная укладка, инвариант
  cellSize ≥ Exit, property set-equality, разложение прогноза). Бюджет ревью плана: 1/2.
- S5 (2026-09-18): 10/10 закрытий раунда 1 подтверждены (сверкой по коду); одна новая
  F11 (мажор, класс F2/F6 — реюз слота в окне при covered-наблюдателе: сравнение жильца
  из хвоста fullPass выпало из новой чистки). Закрыта в плане: seatBySlot (cell+ent),
  чистка (в) — единственный источник Remove (сравнение вечного id жильца), gone-ветка
  dirty изымается, чистка на всех путях включая reconcile; тест-кейсы расширены
  (п.9). Микро-примечания внесены (Grid до recordOf — п.6; reconcile — п.4; константа
  8192 — единая точка NewGrid, п.1/6). Контрольный обмен с замыкателем: F11 закрыта
  полностью, полнота Remove/Introduce/Update после перестройки корректна (разбор всех
  случаев gone-бита: деспавн/реюз вне/в окне/хвостовые дырки; смена ячейки целью —
  ложных Remove нет, дизъюнктность (б)/(в) полная). F12 (минор, формулировка) закрыта
  правкой текста; gone-битмап — осознанное оставление (п.2). Механика при закрытии
  F12 не менялась — второй проход не требуется. Бюджет ревью плана: 2/2 — выход на
  владельца.
- S6 (2026-09-18): свежий взгляд — «состоятельно, конкретно лучшей альтернативы не
  найдено»; рассмотрены и отклонены квадродерево (стенсил перестаёт быть константным,
  инвариант кольца ломается, pointer-chasing), транспонированный cell-major push
  (симметричная стоимость, мутабельный индекс с чурном), ячейка=Exit/аннулярный скан
  (тюнинг/микро-оптимизация). Инвариант cellSize ≥ Exit назван сильнейшей стороной
  (корректность — одно арифметическое неравенство). Микро: абсолютный счёт аллокаций
  блоба с битмапами ~8–9 (не 3–4 — формулировка п.2 про независимость от числа ячеек);
  9×log(#ячеек) на наблюдателя поглощено коридором прогноза. Рестарт не потребовался
  (0/2). Ревью плана ЗАКРЫТО: ноль подтверждённых блокеров/мажоров.
- S8 раунд 1 (2026-09-18): панель = профиль S2 (инвариантщик, перформансер,
  доменщик) + тестировщик (двусторонняя трассировка) + тест-практик; объект —
  диф 1e6c792. Трассировка: план→артефакт полный, «вне плана» пусто, кейсы
  S6b 34/34 реализованы, сирот нет, ассерты поведения старых тестов
  байт-идентичны (F16-норма). Мутационный отчёт: 8 мутаций, все ключевые
  фальсификаторы краснеют (F17-фальсификатор жив — M-G). Вердикты: доменщик
  0/0/2, инвариантщик 0/0/2, тестировщик 0/0/3, тест-практик 1 мажор + 4
  минора, перформансер 1 мажор + 6 миноров → F19–F33. Правки раунда: F19
  (Event-указатель; лестница 10000 50.9→34.9/35.6, 20000 →76.8/76.1 — бюджет
  с запасом), F20/F21 (сортировка free-list — детерминизм «младшего
  свободного» + инвариант-тест чередующихся деспавнов), F22 (property 100
  итераций + края домена), F23 (стрим 200 юнитов, moves≥12), F24 (мёртвый
  код), F26 (enter-дуга инварианта явно + декод-домен), F27 (честная
  раскладка фальсификатора, dirty-путь без Apply: 102/121/126 нс),
  F28/F29/F30 (артефакты перегнаны одним бинарём, атрибуции исправлены,
  обе медианы 20000 явно), F31 (slices.SortFunc, Idle 17→16), F32
  (абсолют 19/1000). F25 (домен 0..0x7FFF) и F29 (Build +20% против
  прогноза) — записи-отклонения. F33 — вне дифа, кандидат в слайс фазы 4.
  Бюджет ревью реализации: 1/3.
- F9/S6b (2026-09-18): до-базлайн join/publish/read перегнан на целевой машине —
  benchmarks/P4.1-replica-before-baseline.txt (медианы: FullPass 515 мкс, SteadyDirty 190 мкс,
  PublishBlob 13.5 мкс/146 мкс/1.36 мс на 100/1k/10k, AdvisoryRead 120 нс/13.4 мкс). S6b
  (тест-планировщик): 31 кейс (17 ПБ), трассировка критериев без пробелов, корнер-чеклист
  пройден, анти-петля по реестрам P3.8/P3.9/P3.10 (твины фантомов/стрима/переполнений),
  швы — достаточны (конструкторский Grid + error-ветка фабрик-инварианта). Четыре находки
  на спеку F13–F16 (минор, оракулы/методика/синтетика/норма «без правок») — подтверждены,
  закрыты формулировками тест-плана. Вход S7 открыт.
