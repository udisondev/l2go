//go:build race

package world

// raceEnabled — сборка с детектором гонок: стрессы с тайминговыми
// фальсификациями (пробой FAF-капа на скорости) под детектором в 5–20×
// медленнее — дрен успевает за волной; инварианты счётчиков остаются.
const raceEnabled = true
