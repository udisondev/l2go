// Package protocol — бинарный слой протокола Interlude 746: каталог опкодов
// (494 записи по шести группам направлений), примитивы кодека и типизированные
// пакеты-образцы. Форма каждого пакета определяется ролью потребителя, а не
// направлением пакета: кодирование исходящего — функция-писатель
// func(dst []byte, args ...) int, пишущая с опкодом в буфер вызывающего и
// возвращающая число записанных байт (малые пакеты — стек вызывающего,
// крупные — пул буферов; размер строковых полей считает LenS); разбор
// входящего — представление type Name []byte с конструктором, проверяющим
// минимальную длину один раз, и геттерами по офсетам без копий. Нулевое
// значение представления недействительно: доступ к полям — только после
// успешного конструктора. Читатели примитивов возвращают (значение, ok) и не
// паникуют на недоверенных буферах; писатели паникуют с диагностикой только
// при нарушении программного контракта (короткий dst).
package protocol
